# RTA-Hub 改进建议清单

来源：第三方测试报告（<https://rta-hub-test.vercel.app/>，9 个 BUG / 5 个测试场景）核实结果。
本文件只收录**本轮已核实但未在代码里修复**的项——已确认为真且已修复的 bug 见下方
「本轮已修复」小节末尾的 PR/commit 记录，不重复放在这里；已核实为「按设计工作/无法
复现」的项也记在这里，附核实结论，避免以后重复排查同一个已经查清楚的问题。

## 本轮已修复（供交叉核对，非待办）

| 报告编号 | 问题 | 修复方式 |
| --- | --- | --- |
| BUG-001 | 模板假设被当作用户输入（Wall North=144" 等未经确认就显示） | 上一轮 PR 已彻底取消模板数值预填，只建墙面结构；本轮验证复现，确认已修复 |
| BUG-003 | 家电尺寸重复提问 | 根因是 `widthNear`（`chat-appliance-answers.ts`）在"宽度在前"语序下，数字与家电关键词之间只留 12 字符窗口，装不下"French Door"这类常见品类描述词，导致客户明明报过尺寸却被静默丢弃、退回推定值。已放宽到 32 字符并补充回归测试 |
| BUG-004（墙长部分） | U 型客户用系统自己教过的"Main Wall/Left Wall/Right Wall"说法回话，被拆成两段多余的假墙（"Left"=101、"Right"=96），"Main Wall 108"完全丢失 | 新增 `SHAPE_WALL_ALIASES`（`src/samples/templates.ts`），U 型的 main/left/right 按户型解读文案里教过的说法对应到 North/West/East，`applyChatWallLengths` 优先查这份表 |
| BUG-008（省份部分） | 客户随口问一句"你们在 Surrey 有安装服务吗"，账号省份被从 Ontario 误判成 British Columbia | `matchProvince`（`src/design/province-match.ts`）新增"问别处有没有服务"句式识别，这类提及不再参与省份判定 |

## 待办 / 改进建议

### 1. BUG-002：家电宽度"推定"机制——核实结论：按设计工作，非 bug

系统选好家电种类后确实会先用北美常见宽度（冰箱 33"、灶具 30"、洗碗机 24" 等）占位，
但这些值全程标 `provenance: "assumed"`，已确认面板显示"推定"标签，且 `readiness.ts`
的 `appliances_sizes` 关键项在客户明确给出实测宽度或说"推定可以"之前会一直挡住出图
（`test/appliances.test.ts` 等已有充分覆盖）。这是刻意的产品设计（先给一版能继续走
下去的起点，出图前必须显式过一道确认），不是"假装已确认"的 bug。

**改进建议**（体验优化，非纠错）：
- 家电种类选定后，主动追问一句"实测宽度是多少？不确定的话我们先用常见尺寸 33"
  占位，出图前需要你确认"，比现在"先默默填上、之后才用 Q# 追问"更早让客户知道
  这是占位值。

### 2. BUG-004（非标尺寸部分）：Sink Base / Filler 等橱柜级组件未被识别

用户输入 `31.5" Sink Base` / `1.5" Filler` 时系统没有任何反应。

**核实结论**：这类词是**橱柜/五金层面的组件规格**（水槽柜、填充板），不属于现有的
"户型墙面特征"模型（窗/门/上下水/燃气/强电）或"家电"模型（冰箱/灶具/洗碗机等），
现有解析器（`chat-site-answers.ts` / `chat-appliance-answers.ts`）压根没有承接这类
输入的字段可写。这不是一个可以"改几行正则"就修好的 bug，而是需要先决定这类信息
该落在哪个数据结构里（`FloorPlan`？未来的柜体清单草案？）才能开始识别。

**改进建议**：
- 明确产品范围：现阶段"水槽柜"实际对应的是 `plumbing` 特征的宽度（`24"` 默认宽度
  已经在用，客户报 `31.5" Sink Base` 时至少应该能识别成"上下水宽度 31.5""，这个
  子集是可以较快补上的）。
- "Filler"（填充板）目前完全没有承接位置——这类信息通常要到排布/BOM 阶段才需要，
  建议评估是否需要在户型采集阶段就收集，还是留到出图后的排布调整环节再补。

### 3. BUG-005：复杂组件（Tall Pantry Cabinet、Crown Molding 等）未被识别

**核实结论**：与上一条同源——现有模型没有"橱柜级组件清单"这个概念，`Tall Pantry
Cabinet` `Crown Molding` 这类词目前不会被任何解析器处理，也不在 `readiness.ts`
检查表的任何一项里。

**改进建议**：作为独立的产品需求排期，而不是零散打补丁：
1. 先梳理清楚客户在户型采集阶段真正需要报的橱柜级组件有哪些（高柜、装饰线条、
   填充板……），哪些是必须在出图前拿到的，哪些可以留到排布阶段由系统按标准规则
   自动决定。
2. 参照现有 `ApplianceSpec`/`WallFeature` 的 `provenance` 门禁模式设计一套新的
   "组件规格"结构，而不是塞进现有的墙面特征或家电模型里（两者语义都对不上）。

### 4. BUG-006：墙长数值校验（极小值 / 负数）——核实结论：部分已有校验，体验可加强

`parseWallLengthInches`（`src/design/chat-site-answers.ts`）已经会拒绝
`< 24"` 或 `> 360"` 的墙长——"Main Wall 24 inches"（边界值，含）会被接受，
"Main Wall = -50 inches"这种带负号的输入，因为解析墙长的正则不认负号，会在
"墙名 + 数字"这一步直接匹配失败，等同于"没听懂"，既不会写入几何，也不会报错，
客户看不出系统到底有没有收到这句话。

**核实结论**：不是"接受了不合理的值就直接用"，而是"识别失败时太安静"——
数据不会被污染，但客户体验上感觉像是"输入被无视了"。

**改进建议**：
1. 给"识别到了墙名 + 一个数字，但数值不在 [24, 360] 合理范围内（含负数）"这种情况
   加一条明确的澄清回复，例如："北墙 -50" 这个数字不太对，麻烦确认一下实际长度
   （一般在 24"–360" 之间）"，而不是完全沉默。
2. 24" 这个下限目前对所有墙一视同仁；是否需要针对"厨房主墙"给一个更高的软下限
   （比如低于 60" 提示"确认一下是不是这么窄"）取决于产品判断，建议单独评估，不要
   直接改硬上限——非常窄的过道墙/填充段仍然是合法输入。

### 5. BUG-007：物理空间冲突检测——核实结论：无法复现，功能已存在且正常工作

用报告给的原始场景（单墙 60"，家电 36"+30"+24"=90" > 60"）实测：系统正确弹出
"⚠ Space check: with the wall lengths and appliance widths you just gave,
Range needs 30" but no wall run can fit it..." + "These appliances need more
wall length than you have."，阻断机制生效，符合预期。未发现需要修复的问题；
测试报告里"仅在特定条件下提示"的现象没能复现，可能是被测版本较旧或触发顺序不同，
建议下次复测时附上完整的请求/响应日志方便定位具体触发条件。

### 6. BUG-008（Off-topic 部分）：非厨房设计问题没有专门的回应机制

**核实结论**：目前系统完全没有独立的"off-topic 识别"模块——碰到"What cabinet
colors do you have?"这类问题，全靠系统提示词里给对话模型的几句指引（"不知道具体
颜色就引导 @ 厂商"之类），本地无 LLM 环境下走的兜底文案也确实不会正面回答，而是
继续追问缺口信息；报告里说"AI回答内容错误假设了省份"的那部分已经用 BUG-008
的方式定位到根因（城市名误覆盖省份）并修复，但"完全不回答/不承认这是 off-topic
问题"这部分依赖真实 LLM 是否严格遵守提示词，本地环境无法验证。

**改进建议**：
1. 给几类高频的 off-topic 问题（门板颜色、服务范围、安装费用等）加一层**确定性**
   的兜底识别 + 标准回复（比如直接告知"颜色由具体厂商决定，请 @ 厂商名"），不要
   完全依赖 LLM 的提示词遵从度——这与现有 `rta-disclosure.ts`（"RTA 与定制的区别"
   这道题不交给模型答）是同一个思路，值得复用。
2. 补充针对真实 LLM（而不是 `llm: undefined` 兜底路径）的集成测试，用 mock
   CompletionClient 验证提示词本身对 off-topic 场景的指引是否足够明确（现有
   `test/template-confirm-briefs-leak.test.ts` 已经演示了这种"接管 LLM 客户端、
   抓取真实提示词"的测试写法，可以复用同样的模式）。

### 7. BUG-009：户型图（英文提示语）识别完全失效——核实结论：本地环境无法复现，需要线上日志定位

代码审查确认：视觉识图调用（`src/floorplan/ollama-vision.ts`）不依赖客户当前对话
语言——无论客户用中文还是英文触发上传，发给视觉模型的提示词都是同一份（英文），
识图结果理论上不应该因为客户打字用的语言不同而系统性失败。本地沙箱没有配置视觉
模型后端（`OPENAI_BASE_URL_VISION` 未设置），无法复现报告里"英文完全失败、中文
部分成功"的现象。

**改进建议**：
1. 需要拿到 Vercel 部署环境的实际请求/响应日志（尤其是 `extraction.status`/
   `extraction.reason`，见 `parse.ts` 的 `ExtractionOutcome`）才能定位——很可能
   是这两次测试恰好用了不同难度的图片，或者线上视觉后端在某次调用时超时/报错，
   而不是语言本身导致的系统性问题。
2. 建议在 `test/vision-pipeline-ab.test.ts` 里补一组"英文 hint / 英文 language"
   的用例，跟现有中文用例并列跑一遍，至少能确保**我们自己代码里**没有语言相关的
   隐藏分支（目前审查未发现有，但加上测试能防止以后引入回归）。
3. 排查生产环境的视觉模型（Ollama + qwen2.5vl）超时设置是否足够（`timeoutMs`
   默认 180s）、以及 PaddleOCR 外援（如果配置了）是否在某些请求下拖慢或拖垮了
   整体识图流程。

## 复测建议（给下一轮第三方测试）

- 请在报告里附上**每一步实际发出的 HTTP 请求体**和**完整响应 JSON**（不只是截图），
  方便逐条对照代码复现，本轮排查中好几个问题都是靠"猜测大概是哪个正则/哪段逻辑"
  才定位到的，有原始请求会快很多。
- 户型图测试请同时附上上传的原图文件，纯文字描述"用了什么图"不足以复现视觉识图
  相关的问题。
