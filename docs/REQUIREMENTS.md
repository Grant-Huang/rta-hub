# 加拿大橱柜报价平台 —— 需求说明书（v0.8）

> **v1.0 变更摘要**（第二轮真人测试的回归）：
> 1. 新增 **§3.5.5.1 箱体板材**：柜体用什么板（颗粒板 / 夹板 / 实木）与门板花色是
>    **两个独立的维度**，各自计价。没有箱体的件（填缝条、踢脚板、塑料地脚）不加价。
> 2. 新增 **FR-1.3 RTA 预先告知**：开场、对话中被问到时、以及报价单末尾都要说清
>    「这里报的是 RTA 板式待组装橱柜，不是全定制」。措辞只有一份
>    （`quote/rta-disclosure.ts`），而且这道题**不交给模型答**。
> 3. 修掉三个从界面上才看得出来的缺陷：选择题按钮点了没反应（数据与卡片脱节）、
>    禁用的按钮不说自己在等什么、**上传户型图时图片内容根本没传上去**
>    （视觉抽取因此一次都没跑过）。

> **v0.9 变更摘要**：全局俯视图从"几段墙各画各的"改成**一张连通的平面**
> （FR-4.3.1～FR-4.3.4）：内墙角让位与转角柜、门洞旁的台面外伸净空、岛台与
> 过道净空，外加交付前审核新增的**跨墙段干涉**阻断项（FR-14）。
>
> **v0.8 变更摘要**：新增 **FR-14 交付前审核**——把结果交给客户之前，
> 按约束与规则严格过一遍，阻断项一条都不放行。详见下方 FR-14。
>
> **v0.7 变更摘要**（家电建模、产品目录、绘图统一）：**十条已全部落地**
> （最后合上的两条是 FR-6.3 换花色比价与 §3.5.5 `finishDependent`，见 DEV_PLAN M5-9/M5-10）。
> 1. 新增 **FR-3.2 家电信息采集**：家电是客户提供的**输入**，不是写死的常数。
>    要问种类、尺寸、想放的位置；客户答"不确定"是合法答案，但推定值必须
>    在图纸解释里如实标注（`provenance` 字段）。详见 [APPLIANCES.md](./APPLIANCES.md)。
> 2. 新增 **FR-4.6 家电配套柜**：冰箱上柜、烟机上柜、烤箱高柜、灶下柜、
>    冰箱侧收口板。商家没有对应型号时**报出来**，不静默留空当。
> 3. 新增 **FR-5.2 图纸标注规范**：每一张图上，水槽与每个家电位**必须有文字标签**；
>    水槽要画出盆的轮廓，且必须落在水槽柜（SB）上。放不下时用编号 + 图例，不省略。
> 4. 新增 **FR-5.3 统一绘图内核**：全局俯视图与单墙四视图目前是两套独立代码，
>    配色/标注/尺寸链都不一致，看起来像两个软件画的。抽出共用内核。
>    详见 [RENDERING.md](./RENDERING.md) 第 8 节。
> 5. 新增 **FR-6.3 换花色比价**：报价清单末尾给出同一套方案换其他门板花色的
>    重算价。**逐行重算**而不是总价乘系数；填缝/收口/踢脚跟着换色，塑料地脚不换。
> 6. 新增 **§3.5.5 finishDependent**：区分"价格随门板花色变"与"不变"的物料。
>    塑料地脚藏在踢脚板后面，给它编一个高级花色版的价格是凭空加价。
> 7. 新增 **FR-2.2 商家规格差异建模**：**不做通用码↔商家码映射表**，改用
>    **能力标签**（capability）。理由：连体 pantry 与分体叠装 pantry 之间
>    件数、价格构成、外观、安装都不同，映射表存不下这种差异。
>    详见 [CATALOG_MODEL.md](./CATALOG_MODEL.md)。
> 8. 新增 **FR-4.7 叠装吊柜**：高层高住宅的吊柜要拼。这是一个**求解**
>    （给定可用高度与该商家的档位集合），不是一个开关；解不出来要如实说明，
>    不能静默把 48" 变成 42"。
> 9. 明确 **通用目录只产生 `EstimateDraft`，永远不产生 `Quote`**（§3.5.6）。
> 10. 修正一处**呈现缺陷**（不是逻辑缺陷）：模拟报告把「会话内容」整段放在前面、
>     图纸放在后面，读起来像是"客户先确认、后看图"。实际调用顺序是对的
>     （`planReview` 阶段才出俯视图，`approvePlan` 之后才出四视图）。
>     报告改为**单一时间线**，图纸嵌在对话流里。
>
> **v0.6 变更摘要**（会话策略与报价清单）：
> 1. 新增 **FR-4.2 出图前先问，先出全局俯视图**：设计会话分五个阶段推进，
>    `readyToDraw` 阶段问客户「需要我帮你生成设计图吗？」，得到确认才出图；
>    先出全局俯视图供多轮修改，客户认可排布后才出完整四视图与报价清单。
>    **一轮修改必须带上「改了什么」**，改不动的部分如实说明而不是假装改过。
> 2. 新增 **FR-4.3 全局俯视图**：整套厨房拼成一张平面（地柜层 + 吊柜层），
>    墙段走向按厨房拓扑推导，推不出来时标注为「示意排列」。
> 3. 新增 **FR-4.4 塑料地脚方案**：`ToeKickSystem` 两种做法对应两份物料清单。
> 4. 新增 **FR-6.1 报价清单**：逐行给出产品代码/名称/数量/单价/总价；柜体与其门板
>    成组，附件单独成区，分类汇总；门板不编假单价，标明「已含在柜体价内」。
> 5. 新增 **FR-6.2 报价必须覆盖完整物料**：填缝条、踢脚/地脚、收口板此前从未进过
>    报价单——客户拿到的是一份缺料的价格。
> 6. 修正 **FR-4.1 的一处实现缺陷**：水槽落位此前只夹在墙段边界内，会生成
>    「排布器自己随后就判为不合格」的方案。硬约束必须在**排布阶段**参与决策，
>    而不是只在事后打分。
> 7. 端到端模拟 `pnpm simulate` 纳入 CI 冒烟，场景由 LLM 动态生成（无 key 时
>    退回确定性生成器并如实标注）。
>
> **v0.5 变更摘要**（MVP-3 完成后）：
> 1. **关闭四个开放问题**（第 12 节）：PDF 报价单优先级（2）、价格对比表触发时机（3）、
>    户型置信度低的兜底交互（4）、`trade` 账号资质核实（7）。
> 2. **贸易价改为两道门槛**（人工核实 + 有效订阅），且**门槛落在定价链路上**——
>    未过门槛的 `trade` 账号按 `consumer` 定价，绕过界面直接打 API 也拿不到贸易价。
> 3. 新增 **FR-1.1**：价格与偏好改用**选择题**（带选项、带算出来的价格影响），
>    不再开放式提问——客户不是行业内人士，"你的预算是多少"等于让他先做一遍市场调研。
> 4. 新增 **FR-1.2**：后端 **LLM 三层模型分层**（轻量 / 主力 / 视觉），
>    含升级触发条件与降级路径，详见 [LLM_ARCHITECTURE.md](./LLM_ARCHITECTURE.md)。
> 5. 新增 **FR-5.1**：**图纸必须配解释**——「这是什么图」+「为什么这么排」，
>    且解释一律来自实际算出来的结果，不写套话。
> 6. FR-4 补充说明**排布目标函数的三层结构**：硬约束（人体工程，不可违反）、
>    软约束（美观，进目标函数）、造价（目标函数里的一项，不是全部）。
> 7. 新增开放问题 10：**NKBA 净空数值尚未对照官方原文核实**（阻断项）。
>    这些是硬约束，数值错了会误判方案合规性。该项与另外五项一起建成了独立台账
>    [LAUNCH_BLOCKERS.md](./LAUNCH_BLOCKERS.md) + 代码里的启动闸门：
>    **生产环境未核实即拒绝启动**。
>
> **v0.4 变更摘要**（针对 v0.3 review 提出的 10 个问题）：
> 1. 新增 **3.5 定价模型**——把 `pricingRule: { retail, trade? }` 两个标量重构成
>    "（型号 × 门板价格组）价格矩阵 + 修饰项 + 折扣规则 + 税 + 运费"的完整结构。
> 2. 新增 **3.6 版本化与可追溯**——`ProductSpec` 版本化、`Quote` 价格快照、设计修订链、审计事件。
> 3. **FR-8 强化**：价格一律由代码计算，LLM 不得输出任何价格字段；新增 6 项硬校验清单；
>    发送闸门改为服务端状态机（不再接受请求体里的 `confirm` 布尔字段）。
> 4. **修复文档内部矛盾**：已发布规格但未订阅的公司，客户**可以 `@`**，拿到的是 `EstimateDraft`
>    （以 FR-10 的写法为准，场景 A 第 6 点同步改）。
> 5. **计费补齐**：`LeadBillingEvent` 增加去重窗口与争议/退款流程。
> 6. 新增 **FR-13 隐私、同意与数据留存**（PIPEDA），覆盖消费者、trade 账号、商家三类主体。
> 7. **FR-2 增加量化验收标准与降级兜底方案**（结构化模板导入）。
> 8. **MVP-1 调整**：FR-2 保留在 MVP-1，试点公司数从 2-3 家改为**先跑通 1 家**。
> 9. **FR-1 @ 路由改为确定性匹配**，LLM 不参与"这是哪家公司"的判断。
> 10. `CompanyMentionSignal` 增加名称归一化与实体关联。
>
> 另新增 **FR-12 公司发现与邮件列表**（承接 [COMPANY_DISCOVERY.md](./COMPANY_DISCOVERY.md) 的讨论）。
>
> 第 12 节开放问题 1（脸型模板绘制方式）、3（比价表触发时机）等尚未讨论，保留。
> 详细端到端走查见 [SCENARIOS.md](./SCENARIOS.md)，本文档第 4 节只保留索引。
> 仓库现有代码与本版需求的关系见第 10 节。

---

## 1. 业务背景与目标

客户要装修厨房，需要跟多家橱柜公司比价。人工做法是：分别联系每家公司 → 描述户型和需求 →
等对方出图纸和报价 → 反复修改。本系统的目标：客户在**一个对话界面**里完成"设计沟通 → 平面图
确认 → 报价单确认 → 发送给对应公司"的全流程，且设计建议**严格遵守每家公司自己的产品规格**。

商业模式上，这是一个**双边平台**：需求侧（客户）默认免费使用，供给侧（橱柜公司）是主要收费对象，
详见第 8 节。

---

## 2. 角色与参与方

| 角色 | 说明 |
|---|---|
| **消费者客户（Consumer）** | 默认免费账号，终端业主用户，在对话框里描述需求、上传户型图、确认设计与报价 |
| **专业/贸易账号（Trade Account）** | 小型建商/装修公司，付费专业账号，管理多个项目，可见贸易价（见 3.4、FR-11） |
| **总控助手（Orchestrator）** | 对话框默认对话对象，负责通用橱柜设计知识、理解户型和需求、把问题路由给具体公司 Agent |
| **橱柜公司 Agent（Company Agent）** | 每家已入驻公司一个（工程上是同一套逻辑按 companyId 参数化，见 3.4），绑定该公司的产品规格库，只回答/只设计"自己公司能做到的" |
| **橱柜公司（Company）** | 现实中的橱柜厂商/经销商，即平台的供给侧租户，最终报价邮件的接收方 |
| **公司运营/店长** | 使用"公司入驻规格录入会话"整理自家产品规格的人 |
| **平台运营/管理员** | 平台自己的员工：维护公司发现库、社媒投放、招商外联、处理计费争议（见 FR-12、第 8 节） |

---

## 3. 核心概念

### 3.1 多 Agent 对话架构

- 客户默认与总控助手对话（通用设计知识：风格/预算/大致想法）。
- 客户可用 `@公司名` 把具体问题路由给该公司 Agent（如"@枫叶橱柜 你们有 36 寸转角柜吗"），
  同一句话可以 @ 多家公司做横向提问。
- 公司 Agent 的回答**必须基于该公司规格库**，不得编造/借用其他公司信息。
- **路由是确定性的，不由 LLM 判断"用户指的是哪家公司"**（见 FR-1）——在一个以数据隔离为
  硬性要求的系统里，"客户以为 @ 的是 A、实际路由给了 B"是不可接受的故障模式。

### 3.2 公司产品规格库（Product Spec）

每家公司的规格库至少包含：

- **柜体型号（Module）**：型号码（如 B12/W2430/SB36）、类型（地柜/吊柜/高柜/转角柜/水槽柜等）、
  每个型号的标准尺寸候选集（宽×高×深，离散值）。
- **脸型模板归属**（见 3.3）——决定它在正视图上怎么画。
- **门板样式（DoorStyle）与价格组（PriceGroup）**：门板花色/材质选项，以及每种门板属于哪个
  价格组——这是 RTA 行业定价的核心维度，见 3.5。
- **价格矩阵**：(型号 × 价格组) → 标价。
- **修饰项**：五金选项、配件选项及其加价规则。
- **折扣规则**：贸易折扣、量级折扣等。
- **运费规则**与**适用税区**。

规格库是**版本化**的（见 3.6），不是一份可以随时被就地改写的文档。

### 3.3 渲染架构：脸型模板库 + 参数化拼接

**结论先行：不让 LLM 直接生成图形坐标，也不引入 CAD 引擎或第三方素材库（如 Sweet Home 3D——
其素材本质是 3D 模型的俯视投影，且是通用款式，无法对应具体公司的真实型号，弃用）。**

正确的分工是"代码算布局数字，代码画图"，且利用 RTA（现货组装）橱柜品类本身高度标准化的特点，
把渲染进一步简化成**小模板库 + 参数化拼接**：

1. **布局计算**（确定性算法，非 AI）：给定墙面可用长度（户型图解析得出）+ 该公司规格库里的
   离散尺寸候选集，做贴墙排布（类似一维装箱/cut-list optimization），算出每个型号的精确
   位置（x/y/宽/高/深，单位英寸，精度到 1/4 英寸）。
2. **脸型模板库**（一次性手绘/手写的 SVG symbol，数量级如下，覆盖绝大多数 RTA 配置）：

   | 视图 | 模板数量级 | 举例 |
   |---|---|---|
   | 正视图 Front | ~10-15 个 | 单门柜、双门柜、三抽屉、一门一抽、转角盲柜、水槽柜、单门/双门吊柜、开放格 |
   | 俯视图 Top（地柜层/顶柜层分开出图）| ~3-5 个 | 外框矩形、转角柜斜切角、开门弧线示意 |
   | 侧视图 Side | ~2-4 个 | 地柜截面、吊柜截面（同一趟柜子复用同一模板，标准台面高度/净空按行业惯例标注）|

3. **渲染**（v0.4 修正）：**不手绘 SVG 素材，也不采用"归一化 `<symbol>` + `transform` 缩放"**——
   缩放会把门缝这类**绝对尺寸**一起放大（36" 柜的门缝会画成 12" 柜的 3 倍），并使线宽各向异性
   失真。改为**声明式脸型文法（矩形递归行列切分）+ 代码计算绝对英寸坐标**后直接输出 SVG，
   同样零依赖、确定性，且可对切分结果的数字做断言测试。详见 [RENDERING.md](./RENDERING.md)。
4. **材质/花色**不单独建模板——同一脸型模板换填充色/花纹，或直接用标注文字体现材质名称。
5. **面框/无框与门板覆盖方式是渲染参数**：美式面框柜正面有 1-1/2" 实木面框、无框柜没有；
   全覆盖（露 1/4"）/ 半覆盖（露 1"-1.5"）/ 嵌入式（缝 3/32"）三种 overlay 观感差异明显。
   在文法方案里它们是两个参数；若按"每种脸型一张图"则模板数要乘 6 倍。
6. **与"公司入驻规格录入会话"（FR-2）的联动**：录入某型号规格时，系统需额外确定它属于哪个
   脸型模板。RTA 行业型号命名通常自带这个信息，可先规则匹配，匹配不到再对话确认。
   ⚠️ 规则的键必须是 **(前缀, 宽度)** 而不只是前缀——`B12`（一抽一门）与 `B30`（双抽双门）
   前缀相同脸型不同，且宽度分界点因公司而异，故规则表必须可按公司覆盖（详见 RENDERING.md 第 5 节）。

### 3.4 多租户架构（两个维度）

这是标准的双边多租户结构，但租户不是单一维度，要分开看：

**维度一：供给侧租户 = 每家橱柜公司**

严格按 `companyId` 隔离的数据：`ProductSpecVersion`（含草稿/已发布/归档多态，见 3.6）、入驻
会话历史与原始上传资料（对其他公司必须不可见——公司的价目表本身有竞争敏感性）、该公司收到的
线索、生成的报价单、计费/订阅状态。

隔离要求：不能只在应用代码里"记得按 companyId 过滤"，要在**数据访问层**兜底强制（每张表带
`tenant_id`/`companyId`，查询层强制加过滤条件，或用支持行级安全的数据库做兜底）——应用层过滤
是第一道防线，数据层隔离是第二道防线，两道都要，这是 FR-8 硬校验原则在多租户场景下的延伸。

"每家公司一个专属 Agent"是产品体验层面的说法，**不是**要为每家公司部署独立的 Agent 进程——
那样公司一多，运维复杂度线性爆炸。工程实现上是**一套共享的 Agent 运行时/prompt 模板，按
`companyId` 参数化调用**，加载对应公司的规格库作为上下文。

**维度二：需求侧账号 = 消费者 / 专业（贸易）**

`CustomerAccount` 分两种：
- **consumer**（默认，免费）：一个账号通常对应一个进行中的装修项目。
- **trade**（付费专业账号，面向建商/装修公司）：一个账号下管理**多个** `Conversation`（多项目
  并行），且按贸易折扣规则定价（见 3.5、FR-11）。

`Conversation`（客户对话/项目）本身归属某个 `CustomerAccount`，其 `perCompanyThreads` 只**只读
引用**某个供给侧租户（`CabinetCompany`）的数据，不会写入对方租户——这个单向读写规则是隔离设计
的一部分。

**平台共享层**（不属于任何租户）：脸型模板库（`FaceTemplate`）、冷启动用的行业基准目录
（`GenericCatalog`，见 FR-10）、加拿大各省税率表（`TaxRule`，见 3.5）、总控 Agent 与核心引擎代码。

### 3.5 定价模型（v0.4 新增，重构）

#### 3.5.1 为什么原来的 `pricingRule: { retail, trade? }` 不够

v0.3 把价格建成"每个型号一个零售价 + 一个可选贸易价"两个标量。这与 RTA 橱柜行业的真实定价
结构不符，具体差在四处：

1. **价格的主维度是（型号 × 门板价格组），不是型号本身。** 同一个 `B12` 地柜，配基础款
   Shaker White 和配 Maple Glaze 是两个完全不同的价，差价常在 40%-80%。原模型的
   `finishes[].priceModifier`（一个加法/百分比修饰）表达不了这种矩阵关系——真实价目表就是
   一张"行=型号、列=价格组"的二维表。
2. **缺税。** 加拿大按省不同（HST / GST+PST / GST+QST / 仅 GST），报价单不列税基本没法给
   客户看，客户拿到的总价会和实际付款差 5%-15%。
3. **缺币种、运费、有效期。** 报价单没有 `validUntil` 就等于承诺永久价格；没有运费就少算一笔
   常见的几百加币。
4. **缺折扣的表达能力。** 贸易价在行业里通常表达为"标价的 X% off"，而且常按订单金额分档，
   还可能只对部分价格组生效——一个 `trade` 标量装不下。

#### 3.5.2 结构

价格的计算链路是确定的，分五层：

```
标价      PriceMatrixEntry(型号, 价格组).listPrice
  ↓ ＋
修饰项    HardwareOption / AccessoryOption / 组装加价 / 非标尺寸加价
  ↓ －
折扣      DiscountRule（贸易折扣、金额分档折扣）
  ↓ ＝    行小计 → 单据小计 subtotal
  ↓ ＋
运费      ShippingRule
  ↓ ＋
税        TaxRule（按客户所在省）
  ↓ ＝    total
```

**核心实体**（完整字段见第 6 节）：

- **`PriceGroup`**（属于公司）：价格组，如 `A / B / C` 或 `Standard / Premium / Designer`。
- **`DoorStyle`**（属于公司）：门板样式，如 "Shaker White"，每个门板样式**属于且仅属于一个
  价格组**。客户在一个报价单里选定一种门板 → 整单落在该门板对应的价格组上。
- **`ModuleSpec`**（属于公司）：柜体型号，含离散尺寸候选集与脸型模板归属。
- **`PriceMatrixEntry`**（属于公司）：**(moduleId × priceGroupId) → listPrice**，这是价格的
  唯一真相来源。可选 `tradePrice`（少数公司确实维护两张矩阵，而不是用折扣表达）、
  `assembledUpcharge`（RTA 未组装 vs 已组装的差价）。
- **`HardwareOption` / `AccessoryOption`**（属于公司）：五金与配件，加价规则支持定额或百分比，
  可限定适用的型号类型。
- **`DiscountRule`**（属于公司）：折扣规则，按受众（retail/trade）、按订单金额分档、可限定
  只对某些价格组生效。
- **`ShippingRule`**（属于公司）：定额 / 满额免运 / 按地区。
- **`TaxRule`**（平台共享）：按省的税率组成，带生效区间。

#### 3.5.3 加拿大税率（`TaxRule` 的初始数据）

税率是会变的（例如 NS 在 2025-04-01 把 HST 从 15% 降到 14%），所以 `TaxRule` 必须带
`effectiveFrom / effectiveTo`，报价时按 `Quote.createdAt` 取当时生效的那条。下表是**初始
录入数据**，不是硬编码常量：

| 省/地区 | 构成 | 合计 |
|---|---|---|
| AB, NT, NU, YT | GST 5% | 5% |
| BC | GST 5% + PST 7% | 12% |
| SK | GST 5% + PST 6% | 11% |
| MB | GST 5% + PST 7% | 12% |
| ON | HST 13% | 13% |
| QC | GST 5% + QST 9.975% | 14.975% |
| NB, NL, PEI | HST 15% | 15% |
| NS | HST 14%（2025-04-01 起，此前 15%）| 14% |

> 上表按撰写时的公开税率整理，**上线前必须由运营重新核对一遍**，且系统要支持在不改代码的
> 前提下更新税率（这正是把它建成 `TaxRule` 实体而不是常量的原因）。

#### 3.5.4 定价引擎（`PricingEngine`）

- 定价是**纯函数**：`price(specVersion, selection, accountType, province, date) → PriceBreakdown`。
  相同输入必须得到相同输出，便于测试与事后复算。
- **LLM 不参与定价的任何环节**（见 FR-8）。模型只负责产出"选了哪些型号、什么尺寸、什么门板、
  什么五金"这一组**选择**，价格全部由引擎算。
- 引擎必须输出**可解释的明细**（`PriceBreakdown`：每一行的标价、每一项修饰、每一档折扣、
  运费、每一项税），而不是一个总数——报价单要能给客户和公司双方逐项核对。

#### 3.5.5 哪些物料的价格随门板花色变（v0.7 新增）

价格矩阵是 `(moduleId × priceGroupId)`，而 `priceGroupId` 来自门板样式——这意味着
**每一个型号**的价格都随花色变。对绝大多数件这是对的，但有例外：

| 类别 | 随花色变 | 理由 |
|---|---|---|
| 柜体（含门） | ✅ | 门板就是花色本身 |
| 填缝条 `filler` / 收口板 `panel` / 踢脚板 `toeKick` / 顶线 `crown` | ✅ | 要与门同色，这正是它们存在的理由 |
| **塑料地脚 `leg`** | ❌ | 藏在踢脚板后面，**没人看得见**。黑色塑料件，一个价 |
| 五金 / 配件 | ❌ | 已经是独立的 `HardwareOption` / `AccessoryOption`，本来就不走价格矩阵 |

给 `ModuleSpec` 增加 `finishDependent?: boolean`（缺省 `true`）。对
`finishDependent: false` 的型号，价格矩阵只需要一行，**FR-8 的价格矩阵完整性校验
要相应放宽**——否则会把"本来就不需要多行"误报成价格空洞。

给一个看不见的塑料件编一个"高级花色版"的价格，是凭空加价。

#### 3.5.5.1 箱体板材：与门板花色是两个独立的维度（v1.0 新增）

客户问的是**两件事**：「柜门长什么样」和「柜子本身用什么板」。

同一款 Shaker White 门板，配颗粒板箱体和配全夹板箱体是**两个价**，
行业里全夹板升级通常报 15%~30%。把它塞进门板花色里，意味着价格组数量翻三倍
（每种花色 × 每种板材），而商家给的价目表根本不是那么组织的：
他们给**一张门板价目表**，外加**一句「全夹板 +20%」**。

所以按商家给的样子建模：

| | 定价上的位置 | 谁决定 |
|---|---|---|
| 门板花色 `DoorStyle` | 决定 `priceGroupId` → 查价格矩阵 | 客户选 |
| **箱体板材 `BoxMaterialOption`** | **作用在标价上的修饰项**，与五金/配件同口径 | 客户选，没选按商家默认档 |

三档归一化为 `code: particleBoard | plywood | solidWood`。商家的叫法各不相同
（「全夹板箱体」/「Baltic Birch Plywood」/「Northwood All-Ply」），排序与
英文 PDF 上的写法按 `code` 走，不按名字——名字只用于给客户看。

**没有箱体的件不加价**：填缝条、收口板、踢脚板、顶线是一块板，塑料地脚是一个
塑料件，谈不上"用什么板做盒子"。判定在 `spec/carcass.ts`，与 3.5.5 的
`finishDependent` 是同一形状的问题、同一种做法（判定只写一处，定价与校验共用）。

两条必须在文字上说清的：

1. **报价单上要写明按的是哪一档。** 客户拿它去跟另一家比，那一家报的可能是
   颗粒板箱体——不写，两个数看起来就是同一件东西的两个价。
2. **"整单涨 18%"是错的说法。** 逐行的柜体涨 18%，但填缝条、塑料地脚不涨，
   所以整单涨幅低于 18%，而且随花色略有不同。要说就说"柜体涨 18%，配件不涨"。

#### 3.5.6 通用目录只预估，不报价（v0.7 明确）

`GenericCatalog`（FR-10 冷启动用）**永远不产生 `Quote`，只产生 `EstimateDraft`。**

它没有型号码、没有价格矩阵、没有商家；它给的是一个区间和一句"实际价格以具体商家
报价为准"。这条已经是当前实现的行为，但从未被写成一条规则——写下来是为了防止
将来有人"顺手"拿通用目录去补一个缺货商家的价格洞。

（`GenericCatalog` 目前的数据是占位的，`sourceNote` 已如实标注，且是上线阻断项 A4。）

### 3.6 版本化与可追溯（v0.4 新增）

v0.3 在第 7 节写了"报价单发送前的每一版设计/报价都应可追溯"，但数据模型里没有任何东西支撑它。
本节把它落成三条具体机制：

**（1）规格版本化：`ProductSpecVersion`**

`ProductSpec` 不再是一个带 `status: draft | published` 的可变文档，而是**一串不可变版本**：

- 每次发布产生一个新的 `ProductSpecVersion`，`versionNo` 单调递增，发布后**不可再修改**。
- 一家公司在任一时刻可以同时存在：**恰好 0 或 1 个 `published` 版本** + **0 或 1 个 `draft`
  版本**（正在编辑的下一版）+ 任意多个 `archived` 历史版本。这正是 3.4 节说的"草稿/发布两态
  并存"——单个 status 字段做不到，必须版本化。
- 公司 Agent 与定价引擎**只读 `published` 版本**；草稿对客户端完全不生效。

**（2）报价快照：`Quote` 冻结价格**

`Quote` 不能只存 `moduleRef` 然后到用时再去查价——那样公司一改价，历史报价单的金额就漂了。
`Quote` 必须冻结：

- `productSpecVersionId`：本单基于哪一版规格。
- 每个 `lineItem` 的 `unitListPrice` / `unitNetPrice` / 各修饰项金额，全部是**下单当时的快照值**。
- `accountTypeAtQuote`：账号类型快照（防止客户从 consumer 升级成 trade 后历史单价格变化）。
- `taxRuleSnapshot`、`shippingRuleSnapshot`、`discountRuleIds`：同理。
- `validUntil`：报价有效期，过期后 `status` 转 `expired`，需重新生成。

复算校验：任何时刻都可以用 `productSpecVersionId` + 冻结的 selection 重跑 `PricingEngine`，
结果必须与快照一致——不一致说明数据被篡改或引擎有回归，属于事故。

**（3）修订链与审计事件**

- **`DesignRevision`**：客户每次通过对话提修改意见，生成一个新的修订版本（`revisionNo` 递增），
  不覆盖上一版。FR-4 说"重新计算受影响的那部分"指的是计算过程，不是可以就地改写历史。
- **`QuoteAuditEvent`**：报价单生命周期里的每个关键动作（创建 / 修订 / 校验拒绝 / 客户确认 /
  发送成功 / 发送失败）都记一条，带当时内容的哈希。这是事后处理计费争议（见第 8 节）时唯一
  可信的凭据。

---

## 4. 端到端场景走查（索引）

详细的场景走查见 [SCENARIOS.md](./SCENARIOS.md)，本节只列清单：

| 场景 | 覆盖角色/租户 | 对应 FR |
|---|---|---|
| A. 公司入驻 | 供给侧租户 | FR-2 |
| B. 冷启动通用层体验 | 消费者账号 + 平台共享层（无需已入驻公司） | FR-10 |
| C. 个性化对话与 @ 路由 | 消费者账号 + 已入驻公司 | FR-1 |
| D. 户型图上传 → 四视图 | 消费者账号 + 已入驻公司 | FR-3、FR-4、FR-5 |
| E. 多公司比价与报价发送 | 消费者账号 + 多个已入驻公司 | FR-6、FR-7、FR-8 |
| F. 贸易账号多项目管理 | 专业账号 | FR-11 |
| G. 贸易账号贸易价可见性 | 专业账号 + 已入驻公司 | FR-11 |
| H. 未入驻公司的销售信号 | 消费者/专业账号 + 未入驻公司 | FR-10 |
| I. 公司发现与招商引流 | 平台运营 + 未入驻公司 | FR-12 |

---

## 5. 功能需求（FR）

### FR-1　对话与 Agent 路由

- 单一对话框内多轮对话，维持通用上下文（需求摘要、已上传户型图、已选公司等）。
- 支持一句话内 `@` 多家公司分别应答。
- 公司 Agent 的知识边界限制在"自己的产品规格（`published` 版本）+ 客户当前项目上下文"。

**路由必须是确定性的（v0.4 强化）：**

- 前端输入 `@` 触发公司联想选择器；用户选中后，消息体里携带**结构化 mention token**
  `{ companyId, displayName }`，而不是一串自由文本。
- 服务端**只按 `companyId` 路由**。
- 用户手打了公司名但没从选择器选（例如粘贴的文本），服务端做**确定性匹配**：规范化后
  （去空格/统一大小写/去 Inc·Ltd·有限公司等后缀/简繁统一）与 `CabinetCompany.name` 及
  `aliases[]` 做精确匹配。
  - 唯一命中且该公司 `status = active` → 路由。
  - 命中多个、或命中 0 个、或命中的公司未 active → **不路由**，由总控助手回复澄清
    （"你是指以下哪一家？"或场景 B 的委婉说法），并记录 `CompanyMentionSignal`。
- **LLM 永远不参与"用户指的是哪家公司"这个判断。** 它可以帮忙生成澄清话术，但不能决定路由目标。
- `aliases[]` 在公司入驻时录入（常见简称、英文名/中文名互译、旧品牌名）。

#### FR-1.1　价格与偏好用**选择题**问，不用开放式提问（v0.5 新增）

原来总控助手对预算和偏好是开放式提问的（"预算大概多少？""喜欢什么风格？"）。这个做法有问题：
**客户不是行业内人士**。他不知道一套橱柜该是多少钱，也不知道 shaker 和 raised panel 差多少。
开放式提问把不该由客户承担的信息负担推给了客户——他要么随口给一个数，要么答不上来卡住。

改为**带选项、带价格影响的选择题**：

| 问题 | 选项来源 | 价格影响的算法 |
|---|---|---|
| 预算档位 | 按户型尺寸锚定的三档 | `GenericCatalog` 典型价 × 估算柜数 |
| 门板样式 | 该公司 `doorStyles` | 各价格组在**公共型号**上的均价差 |
| 组装方式 | RTA / 组装好 | 价格矩阵里 `assembledUpcharge` 的实际分布 |
| 储物偏好 | 抽屉 / 均衡 / 门板 | 取决于最终排布，如实标注 |
| 五金升级 | 该公司 `hardwareOptions` | `priceModifier`（每柜加价，知道柜数时给合计）|
| 功能配件 | 该公司 `accessoryOptions` | `priceModifier` + 可安装的型号清单 |
| 取舍优先级 | 总价 / 用料 / 外观 | 无 |

**两条硬规则**（与 FR-8 同源）：

1. **选项只能来自该公司真实的规格库。** 不存在的门板、这家公司不提供的五金，一律不出现——
   否则客户选了一个买不到的东西。只有一种门板时**不出这道题**，问一个没得选的问题只是浪费一轮。
2. **价格影响一律由代码从价格矩阵算出，LLM 不参与。** 模型可以润色措辞，但"贵 62%"这个数字
   必须是算的。算不出来就如实写「价格待确认（原因）」，不猜。

几个容易做错的细节，都在实现里处理了：

- **价格组溢价只在两组都有报价的型号上比较。** 价格矩阵允许有洞（某型号只在高档组提供），
  直接拿两组各自的平均价相比，等于拿两个不同的篮子比总价——高档组多几个大柜就会显得
  "贵 40%"，而那跟门板无关。交集为空时返回「价格待确认」而不是硬算一个数。
- **没有户型尺寸就不问预算。** 区间需要尺寸锚定，凭空给区间就是编数字。
- **五金/配件按 `appliesTo*` 过滤后才加到选择上。** 加到装不了的柜体上会被 FR-8 校验整单拒绝。
- **偏好按「跨公司通用 / 公司专属」分开存。** 门板/五金/配件的 id 只在某家公司的规格库里
  有意义，换公司就失效；预算档位、储物偏好、取舍优先级则跨公司保留——否则比价时会出现
  "在 A 家选过、B 家没选"，拿两套不同偏好排出来的方案比价。
- **储物偏好进的是排布算法，不是记下来给人看。** 选"尽量多做抽屉"会改变装箱候选的偏好项
  （`LayoutOptions.drawerBias`），进而改变实际排出来的柜型。已生成的方案需要重排才生效，
  界面上如实提示。

#### FR-1.3　预先告知这是 RTA，并解释与定制橱柜的区别（v1.0 新增）

客户脑子里想的往往是「定制橱柜」：任意尺寸、上门量尺、师傅安装、想改哪儿改哪儿。
他买到的是 RTA：尺寸只有几个固定档、板件平装发货、多数情况要自己或另找人装。

**这两件事的差价能到一半以上，但那不是"便宜"，是不同的东西。** 等到货到了才发现，
已经不是解释得清的阶段了——那是投诉和退货。

三处必须说，而且**措辞只有一份**（`src/quote/rta-disclosure.ts`）：

| 时机 | 说什么 | 为什么是这个时机 |
|---|---|---|
| 会话开场 | 短的那一段（`RTA_INTRO`） | 客户还没投入任何东西 |
| 客户问起 | 五项对照（`renderRtaComparison()`） | 尺寸 / 到货状态 / 安装 / 改动 / 价格 |
| 报价单末尾 | `RTA_QUOTE_NOTE` | 签字之前的最后一次；也是他拿去与定制报价对比的那一份 |

**这道题不交给模型答。** 模型的典型失手是把差异说轻（"主要是需要自己组装"），
而恰恰是被说轻的那几条——尺寸只有固定档、不含上门安装——事后最容易变成投诉。
`asksAboutRta()` 认出这类提问后直接给固定答案，判定刻意偏宽：多解释一次没有代价。

措辞上**既不贬低也不美化**：RTA 是正当的产品形态，同样的板材五金，省掉的是仓储、
组装工时和设计师抽成。写清楚它是什么、不是什么，客户自己判断值不值。

#### FR-1.2　后端 LLM 模型分层（v0.5 新增）

后端用**三层模型**，详见 [LLM_ARCHITECTURE.md](./LLM_ARCHITECTURE.md)：

| 层 | 干什么 | 为什么单独一层 |
|---|---|---|
| 轻量 | 日常问答、需求收集、澄清、公司规格事实问答 | 占 90% 的轮次，延迟敏感，单轮价值密度低 |
| 主力 | 需求→设计意图、复杂修改理解、规格模板解析 | 低频，但错了产出的是错的方案与报价 |
| 视觉 | 户型图抽取（FR-3） | 输入是图，文本模型无从下手 |

**视觉模型是必要的，不是可选项**——户型图要读出墙段长度、门窗位置、上下水、层高。
但它的定位是**读取 + 标注不确定，不做决定**：输出一律进 `FloorPlanUnresolved`，
`suggestion` 永不自动采用（FR-3 已有的规则）。尺寸读错 3 英寸，出来的是一份装不上墙的
方案和一份错的报价，而这个错误要到安装当天才暴露。

**升级到主力层的判断必须有确定性兜底**，不能全靠轻量模型自己举手——它判断力不足的时候，
恰恰就是最需要升级的时候。确定性触发（要出方案、多约束修改、户型核对、规格导入）优先于
模型自述，再加一条兜底：连续 3 轮没有新字段被收集就强制升级。

**三条不因分层而放松的约束**：FR-8（任何层都不产出价格）、FR-1 的 `@` 路由确定性
（不经过任何模型）、升级判断的确定性兜底。分层是成本与延迟的优化，不把安全边界也分层。

### FR-2　公司入驻规格录入会话

- 每家公司独立的后台会话入口，可上传文件（产品手册/价格表/规格书，PDF/图片/Excel）。
- 系统持续追问，引导整理出结构化字段：型号×尺寸矩阵、脸型模板归属（见 3.3 第 5 点）、
  **门板样式与价格组归属、(型号 × 价格组) 价格矩阵**、五金/配件选项、折扣规则、运费规则、
  适用税区（见 3.5）。
- 产出人类可读 + 结构化两种形式的"产品规格文档"，公司方可编辑修改，**确认后才发布**——发布
  即生成一个不可变的 `ProductSpecVersion`（见 3.6），未发布草稿不影响线上公司 Agent 的回答。
- **完整性优先于自动化程度**：任何抽取不确定的字段必须进入"待确认队列"并标注置信度，
  系统**不得自行猜一个值蒙混过去**（这是 FR-3 完整性原则在录入环节的延伸）。
- MVP-1 阶段**先完整跑通 1 家公司**（见第 9 节），不追求覆盖率。

#### FR-2 验收标准（量化，v0.4 新增）

以 1 家试点公司的真实价目表为验收对象（RTA 行业常见规模：150-400 个 SKU）：

| 指标 | 定义 | 门槛 | 测法 |
|---|---|---|---|
| **SKU 召回率** | 结构化结果中的 SKU 数 ÷ 原始价目表 SKU 总数 | ≥ 98% | 人工数原表 SKU 总数作分母 |
| **价格准确率** | 抽样 (SKU × 价格组) 组合，金额与原表完全一致的比例 | **= 100%**（抽样 50 条，0 容错）| 双人独立比对 |
| **必填字段覆盖率** | `moduleCode` / `type` / 宽高深选项 / `faceTemplateId` / `priceGroupId` / `listPrice` 六项全部非空的 SKU 占比 | ≥ 95% | 自动统计 |
| **脸型模板规则命中率** | 无需人工干预即正确归属 `faceTemplateId` 的 SKU 占比 | ≥ 70% | 与人工标注基线比对 |
| **脸型模板最终准确率** | 发布前全量 SKU 的归属正确率 | **= 100%** | 人工全量复核（MVP 阶段 SKU 量可承受）|
| **静默失败数** | 系统自行猜测且未标记为待确认的字段数 | **= 0** | 审计草稿里的置信度标记 |
| **人工修正次数** | 从上传到可发布，运营侧手动改动的字段次数 | ≤ 30 次 | 操作日志计数 |
| **端到端耗时** | 上传 → 得到可发布草稿 | ≤ 2 人时 | 计时 |

**价格准确率为什么是 0 容错**：价格错一分钱就是对外发出的错误报价，属于事故级问题，
不能用"95% 准确"这种统计口径来接受。

#### FR-2 降级兜底方案（必须在 MVP-1 一并实现）

如果上述验收不达标，MVP-1 的其他环节（对话、设计、报价、发送、计费）不能被规格录入卡住，
因此**兜底路径与会话式录入一起做**：

1. **结构化模板导入**：平台定义标准 Excel/CSV 模板（型号表、价格矩阵表、门板表、选项表四张
   sheet），公司或平台运营按模板填写后导入，导入时做同样的硬校验。
2. **会话降级为校对助手**：FR-2 会话从"负责从零抽取"降级为"负责把导入结果与原始 PDF 比对、
   指出缺漏与可疑项"——这个角色对 LLM 来说容易得多，且错了也只是漏报，不会产生错价。

#### FR-2.2　商家规格差异建模：不做码映射，做能力标签（v0.7 新增）

每个商家的产品编码不同——但**差的不只是编码，设计本身就不一样**：

- 有的商家的 pantry 上下柜是**连体**的，有的是**分离**（需要叠装）的；
- 有的有 42" 高的墙柜，有的最高只到 39"。

**决策：不建立「通用码 ↔ 商家码」映射表。** 理由是语义不守恒，不是工程量：

一张 `PANTRY84 → ?` 的映射，在连体商家那里是**一个** SKU、一个价格、一扇通高门；
在分体商家那里是**两个** SKU、两个价格、中间一条看得见的横缝、外加一包连接件。
映射表能存"A 的 X 对应 B 的 Y"，存不下"A 的一个变成 B 的两个，而且多一条缝、
多一包五金"。硬塞的结果是表里出现 `PANTRY84 → [PC1842, PC1842] + 连接件`——
那已经不是映射表，是**组合规则**，只是被写成了映射表的样子。

高度档位同理：`W3042` 在只有 39" 的商家那里映射到 39"，就是**默默改了尺寸**——
客户看到的图是 42"，收到的柜子是 39"，顶上多 3" 的缝。这是最坏的一种错，因为
它不报错，它只是错。

**正确的做法：**

1. **报价与出图一律直接使用商家自己的 `code` 与价格矩阵**，系统内部不存在"通用型号码"。
2. **层高是输入，吊柜高度是求解结果**——把约束交给每家自己的档位表去解。
   A 家解出 42"，B 家解出 39" + 更高的顶线，两家都对，两个方案都能报价、都能画图。
3. 排布算法读**能力标签**（`ModuleCapabilities`：roles / servesAppliance / stacking /
   monolithic），不读型号码。能力可以跨商家对齐，编码不能。
4. 能力来源按可信度排序：商家导入时直接声明 > 从脸型模板反推 > 从码前缀猜。
   **猜出来的不能直接进规格库**，走待确认队列（与 FR-2 的「零静默失败」同源）——
   一个把水槽柜误判成普通地柜的规格库，会让每一版方案的水槽都排错位置。

一句话：**编码不映射，能力才映射。**

详见 [CATALOG_MODEL.md](./CATALOG_MODEL.md)。

### FR-3　户型图上传与解析

- 图像理解/OCR 估算房间轮廓与关键尺寸；精度 1/4 英寸颗粒度即可；不确定时对话核对或要求客户
  手动补充；不支持 CAD 源文件解析（引导截图代替）；解析结果必须覆盖所有相关区域，不允许漏项。
- 客户上传的内容不局限于户型照片本身，也可能是此前从别家（含专业设计软件如 2020 Design）拿到
  的报价单/方案截图或 PDF——这些统一走同一条通用抽取管线（图像理解/OCR + 抽不准就对话核对），
  不为特定专业软件格式单独建解析器（普通消费者几乎不会持有 DXF/CSV 这类专业交换格式的原始文件，
  为此单独开发不划算；若未来平台要直接服务专业设计师/经销商这类用户，可重新评估）。

#### FR-3.2　家电信息采集（v0.7 新增）

**家电是客户提供的输入，不是系统写死的常数。**

现在的实现里，冰箱一律按 36"、灶台按 30"、洗碗机按 24" 留空，而且**只有当户型图上
识别出对应特征（强电/燃气）时才会留**。两个后果：

1. 客户的冰箱是 33" 或 36"+2" 通风间隙，按常数留空要么装不下、要么白白浪费储物；
2. 没识别到强电特征就**根本不排冰箱位**——而没有哪家厨房是没冰箱的。

采集内容（作为 FR-1.1 的**选择题**，不是开放式提问）：

| 问 | 选项形式 | 说清它影响什么 |
|---|---|---|
| 有哪些家电 | 多选：冰箱/灶台/烤箱/抽油烟机/洗碗机/微波炉 | 决定要留哪些位、配哪些柜 |
| 各自宽度 | 常见档位 + "我去量一下" + 自填 | 决定留空尺寸 |
| 想放的位置 | 靠近入口/靠近水槽/你帮我定 | 影响整体排布与落台面 |

**「我不确定」是合法答案**，走常见默认值。但推定值必须记为 `provenance: "assumed"`，
并且：

- 图纸解释里如实说「冰箱位按 33" 常见尺寸预留，你的冰箱更宽的话这一版要调整」；
- **由推定值导致的硬约束否决，提示里必须说明它是推定的**——否则客户会以为自己的
  厨房真的排不下。

详见 [APPLIANCES.md](./APPLIANCES.md)。

### FR-4　平面设计自动生成

- 全自动生成初步方案（布局算法 + 3.3 节模板拼接），仅使用目标公司 `published` 规格版本中真实
  存在的型号与尺寸。
- 客户通过对话提修改意见触发局部重新生成，不提供可视化拖拽编辑器（MVP 阶段）。
- 每次修改生成一个新的 `DesignRevision`，不覆盖历史（见 3.6）。

#### FR-4.1　排布目标函数的三层结构（v0.5 补充）

"排得好"不等于"好装 + 便宜"。厨房是每天要用的地方，排布同时要**好用**和**好看**。
这三类要求在算法里的地位**不同**，混在一起会出错：

| 层次 | 内容 | 在算法里的地位 |
|---|---|---|
| **硬约束** | 人体工程与安全净空：水槽两侧落台面、灶台两侧落热锅的地方、洗碗机紧邻水槽、冰箱开门不打架、工作三角 | **不参与权衡**。违反即 `acceptable = false`，出方案接口不出图、报价接口 409 |
| **软约束** | 美观：柜宽节奏、避免凑数窄柜、上下门缝对齐、水槽对窗居中、填缝条靠墙 | 进目标函数，有权重 |
| **造价代理** | 剩余长度、柜体数量 | 进目标函数，**只是其中一项**，不是全部 |

为什么硬约束**不能**做成"权重很大的一项"：任何权重都能被"足够便宜"的方案投票压过。
一个把灶台顶到墙角、省下两百加币的排布，在加权模型里是可能胜出的——但那是不该发给客户的方案。
所以它必须是闸门，不是砝码。

美观**必须**进目标函数（而不是事后提示），因为"填得尽量满 + 柜子尽量少"这两条本身就会
主动制造难看的排布：

```
36, 9, 30, 12, 33   ← 填满了、柜子也不多，但宽度跳来跳去
36, 33, 30, 30      ← 造价接近，观感好得多
```

改造目标函数后的实际变化：78" 墙段从 `36,33,9` 变成 `27,27,24`；87" 从 `36,36,15`
变成 `30,30,27`。实现见 `src/layout/ergonomics.ts`（硬约束）与
`src/layout/aesthetics.ts`（软约束权重）。

#### FR-4.2　出图前先问，先出全局俯视图（v0.5 新增）

原来的流程是「户型一齐全就直接出四视图 + 报价」。两个问题：

1. **没问就画。** 生成设计图对系统只是一次计算，对客户却是一次
   「我要开始认真看方案了」的心理切换——不该由系统替他决定何时切换。
2. **一上来给四张图太多了。** 正视图、两张俯视图、侧视图一起摆出来，客户不知道
   该看哪张、该对哪张提意见。而他这个阶段真正要判断的是「柜子大致这么排，行不行」，
   那是**一张全局俯视图**就能回答的事。

所以设计会话分阶段推进（实现见 `src/design/stages.ts`）：

```
collecting ──(户型齐)──> readyToDraw ──(客户点头)──> planReview ──(客户认可)──> fullDrawings ──> quoted
                                           ↑              │
                                           └──(要改)──────┘
```

| 阶段 | 系统说什么 | 能出什么图 |
|---|---|---|
| `collecting` | 还缺什么 | 无 |
| `readyToDraw` | **「需要我帮你生成设计图吗？」**并说明会先出俯视图 | 无 |
| `planReview` | 请客户看排布、提意见 | 仅全局俯视图（地柜层 + 吊柜层） |
| `fullDrawings` | 完整图纸已出 | 四视图 |
| `quoted` | 可以发给公司或再比一家 | 四视图 + 报价清单 |

规则：

- 从 `readyToDraw` 到 `planReview` **必须由客户的动作触发**。系统不能因为
  「资料齐了」就自己往前走——那正是这次改造要消除的行为。
- **进入 `readyToDraw` 只看户型图是否完整**，不要求聊天里凑齐尺寸/风格/预算等字段。
  户型图本身就是尺寸与布局，省份在账号上，风格与预算是选择题（FR-1.1）。把它们
  设成前置条件，等于让客户为了看一眼排布先做一遍问卷。
- 阶段是**状态的函数**，不取决于有没有人先查过一次状态。
- **一轮修改必须带上「改了什么」。** 只把轮次计数加一、不改任何输入，下一版会画出
  一模一样的图，而客户以为自己的意见被采纳了——那比不支持修改更糟。修改走的是与
  选择题**同一套校验**（`validatePreferences`），并分别记录：
  - `note`：客户的原话；
  - `applied`：真正改到排布上的偏好项；
  - `unapplied`：客户提了但系统落不下去的部分（如「看着大气一点」），**如实说明
    这一版与上一版相同**，不假装改过。

#### FR-4.3　全局俯视图（v0.5 新增，**v0.9 大改**）

`planReview` 阶段出的是**整个厨房拼在一张图上**的俯视图，分地柜层与吊柜层两张
（实现见 `src/render/plan-view.ts`）。这与 FR-5 的单段墙俯视图不同：单段墙的图
回答不了「L 型的两条边怎么接」「U 型中间还剩多宽」，而那恰恰是客户第一眼要判断的。

墙段走向是**推出来的**，不是存的：`WallRun` 只有长度和「两端是否内墙角」，户型抽取
给不出可靠方位。按厨房常见拓扑推——墙段首尾相接成链，遇内墙角转 90°。推不出来
（墙段之间不相接）时如实标注为「示意排列」，不假装知道方位。

##### FR-4.3.1　平面必须是**连通**的（v0.9 新增）

客户的原话：「全局视图要把几个墙围起来、连起来，不能像四个单独的视图那样，
把南墙、北墙、东墙分开写。只有把它们连起来的时候，才会发现开关门的问题、
干涉的问题，以及一些边角柜等细节。」

这不是画法偏好，是**能不能发现问题**的分界线。连不起来的平面上，下面这几类
问题一个都查不出来，因为它们全都跨墙段：

| 只有连起来才看得见 | 查它的地方 |
|---|---|
| 墙角处两段墙的柜子占同一块地方 | `audit.ts` 的 `INTERFERENCE`（阻断） |
| 转角柜该放在哪一段 | `plan-model.ts` 的 `corners` |
| 岛台到四周柜列的过道够不够走 | `ergonomics.ts` 的 `AISLE_TOO_NARROW` |
| 工作三角的三条边到底多长 | `checkWorkTriangle` |

因此平面模型只能有一份（`src/layout/plan-model.ts`）：世界坐标、墙角归属、
围合区、岛台落位、构件足迹都从它出，画图、排布、净空检查、交付前审核都读它。

**墙段相接的默认值**：手工或对话录入的墙段**默认与上一段相接**。人一段一段
报自己家厨房的墙时报的是一圈连着的墙；默认成"不相接"的话，U 型厨房会被画成
三条各自独立的横条，而这正是客户看到的那一版。真的不相接就显式声明。

##### FR-4.3.2　内墙角：让位与转角柜（v0.9 新增）

两段墙在内墙角相接时，**转角那块 24"×24" 只能归一段**：

- **长的那一段拥有墙角**，柜子一直排到墙角，转角柜（`corner` 型号）放在它上面；
- 短的那一段让开「相邻段该层进深 + 3" 转角填缝条」——地柜 27"、吊柜 15"。

填缝条不是余量：不留的话，让位侧的门与抽屉一拉出来就撞上相邻墙柜体的门和拉手，
两个柜子各自"没超墙"，合起来开不了。

按长度而不是按顺序分归属，是因为 U 型的中间段通常最短而水槽多半在它上面；
按顺序分会让中间段又让一次位又摆一个转角柜，可用长度直接腰斩。

##### FR-4.3.3　台面外伸计入净空（v0.9 新增）

台面不是柜体的投影——它前沿比箱体多伸 1-1/2"，外露端头也多伸 1-1/2"。
门洞旁的柜子必须为此让开「台面端头外伸 + 门套线」，否则台面压在门套上，
门开到一半就顶住。**图上要把台面外沿画出来**：让开的那一点距离，客户要看得见。

##### FR-4.3.4　岛台（v0.9 新增）

岛台是 `kind: "island"` 的 `WallRun`——几何上就是一列不靠墙的柜子，装箱、物料、
报价与靠墙的段完全一样。只有三处不同：不生成吊柜层、四面都是外露面、
落位靠围合区而不是墙线。

过道净空按 NKBA：低于 36" **阻断**（人过不去），低于 42" 提示（弯腰开柜门会顶到
对面）。量的是**台面外沿之间**的距离，不是柜体箱体之间——两边各伸 1-1/2"，
按箱体量会算出一条实际上并不存在的合格过道。

#### FR-4.4　塑料地脚方案（v0.5 新增）

踢脚做法有两种，它们不是同一份清单的两种叫法，而是**两份不同的物料清单**：

| | 整体底座 `plywoodPanel` | 塑料地脚 `plasticLegs` |
|---|---|---|
| 支撑 | 柜体自带底座 | 每柜 4 条可调地脚，宽于 30" 的 6 条 |
| 踢脚 | 整长踢脚板贴面 | 扣板夹在腿上（更薄） |
| 优点 | 件数少 | 逐个调平（旧房地面很少平）、运输体积小 |

由 `ProductSpecVersion.toeKickSystem` 声明，缺省 `plywoodPanel`。选错做法，
报价单上就会少几十条腿或多一条用不上的底座。

#### FR-4.6　家电配套柜（v0.7 新增）

家电位不只是"这段墙不能放柜子"。真实厨房里家电周围有专门的柜子：

| 家电 | 配套柜 | 不做的后果 |
|---|---|---|
| 冰箱 | 冰箱上柜（深 24"、高 12–18"）+ 两侧通高收口板 | 冰箱顶上一个积灰空当，侧面露刨花板边 |
| 抽油烟机 | 烟机上柜（中间让位，两侧仍是柜） | 吊柜层排到灶台正上方 |
| 独立烤箱 | 烤箱高柜（中间开洞，单烤/双烤高度不同） | 烤箱没地方装 |
| 嵌入式灶台 | 灶下柜（台面开孔，上层抽屉要避让） | 普通三抽柜的上抽屉会顶到灶 |

两条规则：

1. **配套柜按能力标签查，不按型号码前缀猜**（见 FR-2.2）。排布器问的是
   「给我一个 `servesAppliance === "refrigerator"` 的吊柜型号」。
2. **商家没有对应型号时报出来，不静默跳过**。冰箱顶上留空当是客户会看见的真实结果，
   由他决定接受还是换一家。处理方式与 BOM 缺辅料一致（`bom.ts` 的 `missing`）。

另：**家电位置必须在分层排布之前统一确定**。抽油烟机在吊柜层，位置却由地柜层的
灶台决定；现在两层各排各的，吊柜层不知道灶在哪。

#### FR-4.7　叠装吊柜（v0.7 新增）

高层高住宅（108"、120"）的吊柜可用高度超过单柜档位上限，需要上下拼装。

这是一个**求解**，不是一个开关：给定目标高度 H 与该商家真实存在的高度档位集合，
找一组高度使 `Σhᵢ + (n−1)×接缝损耗 = H`。

- **硬约束**：每一段都必须是该商家真实存在的档位（与 FR-8 同源）；
  上下段必须各自支持叠装。
- **偏好**：段数越少越好；下段更高（重心在下、常用高度在下段）。
- **软约束（跨柜体）**：同一面墙上的横向接缝**高度一致**，否则正视图上是一条锯齿线。
  因此接缝高度要**先在墙段层面定下来，再给每个柜子求解**——不能在逐柜循环里做。
- **解不出来时如实说明**：「你家层高 108"，这家最高的吊柜是 42"，顶上 6" 用加高顶线
  收口。想做到顶需要选支持叠装的商家。」**不能静默把 48" 变成 42"。**

详见 [CATALOG_MODEL.md](./CATALOG_MODEL.md) §3。

### FR-5　三视图渲染（见 3.3 节，已定稿）

#### FR-5.1　图纸必须配解释（v0.5 新增）

四视图对客户是**陌生的表达形式**。他看到一张正视图，不知道那些竖线是门缝还是柜体分界，
不知道俯视图为什么要分地柜层和吊柜层，更不知道「为什么水槽在这儿、为什么这里放了个
9 寸的窄柜」。没有解释的图纸，客户只能凭直觉说「感觉怪怪的」，说不出哪里怪——
修改意见就无从提起。

所以生成图形时必须同时给出两部分（实现见 `src/render/explain.ts`）：

1. **这是什么图**：每张视图在看什么、图上的元素怎么读（门缝、面框、填缝条、
   台面高度、家电预留位）。按公司实际做法说——面框柜就说面框柜，无框柜就说无框柜；
   没有吊柜就不解释吊柜层。
2. **为什么这么排**：水槽与窗的关系、高频区为什么用抽屉、宽度节奏、上下对缝、
   填缝条位置，以及**逐条列出人体工程检查的结果**（通过的也列，客户才知道这些被查过）。

**一条硬规则：解释必须来自实际算出来的结果。** 「水槽对准了窗中心」只有在真的对准时
才说，偏了多少也照实说；美观评分低的项要说出来，不只报喜。这与 FR-8 是同一条原则——
**能算的就不要说**。一句"我们把水槽放在了采光最好的位置"如果不是从几何算出来的，
它就是编的，而客户会据此做决定。

- 正视图、俯视图-地柜层、俯视图-顶柜层、侧视图，共四张图，基于脸型模板库参数化拼接生成。

#### FR-5.2　图纸标注规范（v0.7 新增）

审查模拟产出时发现：**四视图与全局俯视图上都没有标出水槽和家电的位置**。排布器内部
是知道的（`Placement.applianceKind`、`label === "sink"`），但渲染层只写 `moduleCode`，
而家电位没有 `moduleCode`——于是它们在图上是几个**没有任何文字的灰色方块**。

客户看图时第一个找的就是"我的冰箱在哪、水槽在哪"。这两样标不出来，图纸就没法评审，
FR-4.2 说的"多轮修改"也就无从谈起。

硬性要求，**每一张图**（全局俯视图两层、正视图、单墙俯视图、侧视图）：

1. **水槽**要画出**盆的轮廓**（不只是给柜体填个色）+ 龙头位置，并标注「水槽」。
   水槽必须落在水槽柜（`type === "sinkBase"`）上。
2. **每个家电位**必须有文字标签，含种类与宽度：`冰箱 33"`、`灶台 30"`、`洗碗机 24"`。
3. 标签放不下时（柜位太窄）用**编号 + 图例**，不是省略——图上有一个客户看不懂的
   方块，等于这张图在这一处是失效的。
4. **图例从图上实际出现的元素生成**，不写死：没有吊柜的图不该有"吊柜"图例。
   与 `explain.ts` 同一条原则——说的每一句都要对应实际算出来的东西。

#### FR-5.3　统一绘图内核（v0.7 新增）

全局俯视图（`render/plan-view.ts`）与单墙四视图（`render/views.ts`）目前是**两套独立
代码**：各有一份坐标映射、一份转义函数、一套配色；全局图**没有开门方向弧线、没有逐柜
尺寸链**。技术是同一种（都是自己拼字符串出 SVG，没有用任何开源绘图库，理由见
RENDERING.md 第 4 节），但**绘图约定不一致**——而客户看的是约定，所以两组图看起来
像两个软件画的。

要抽出一层绘图内核（画布/图元/图例/配色/标注规则），两个渲染器只负责"摆什么"，
不负责"怎么画"。判定标准：**同一个东西（比如填缝条）在两组图里必须是同一个颜色、
同一种剖面线、同一种标注方式。**

详见 [RENDERING.md](./RENDERING.md) 第 8 节。

### FR-6　报价单生成与多公司价格对比

- 结构化报价单，逐行包含：型号码、数量、尺寸、门板样式与价格组、五金/配件、单位标价、
  折扣后单价、行小计。单据层面包含：小计、折扣明细、运费、**税（分项列出税种与税率）**、
  总价、币种、**有效期**、备注。
- 定价全部由 `PricingEngine` 计算（见 3.5.4），按 `CustomerAccount.accountType` 应用对应的
  折扣规则（见 FR-11）。
- 客户向多家公司询价时，提供价格层面的横向对比表（不做视图/图形对比）。对比表必须注明各家
  报价的**含税口径**是否一致，避免拿含税价和未税价直接比。

#### FR-6.1　报价清单：逐行列出，分组分类（v0.5 新增）

一个总价没法让客户判断贵在哪，也没法跟另一家比。随图纸给出的报价清单必须逐行包含
**产品代码、产品名称、数量、单价、总价**（实现见 `src/quote/line-items.ts`）。

**分组规则：**

1. **每个柜体与它的门板放在一起。** RTA 的柜体 SKU **本身就含门板**——价格由
   「型号 × 门板价格组」决定，门不是单独一行商品。所以柜体行带价格，门板作为
   **从属明细**挂在它下面，写清是哪款门、几扇门/几个抽屉面，并标明「已含在柜体价内」。

   > 为什么不编一个门板单价：我们没有拆分依据。编出来的拆分会被客户拿去跟别家比，
   > 而那个数字是假的。分类汇总里门板一类金额为 0，但**不能显示成 $0**——那会让
   > 客户以为门是送的，必须用文字说明它含在柜体价内。

2. **填缝条、踢脚/地脚、收口板单独成区。** 它们不属于任何一个柜体，服务于整套。
3. **分类汇总**：柜体、门板、五金、功能配件、填缝与收口件，各自件数与金额。
4. **逐行合计必须与报价单小计对得上**，差额非 0 时在清单上明确报出——那说明有行漏了。

门板数量取自渲染那套已经定好的脸型文法（`matchFaceTemplate`），不另起一套规则：
否则会出现报价单写 2 扇门、图纸画 1 扇。

#### FR-6.2　报价必须覆盖完整物料（v0.5 新增）

报价单原先只列柜体（数据来自 `moduleCounts`，而那只统计柜体）。一套厨房装起来还需要：

- **填缝条**：墙长不可能刚好等于柜体宽度之和，差额要有东西补；
- **踢脚**：整长踢脚板，或塑料地脚 + 扣板（见 FR-4.4）；
- **收口板**：外露的柜体侧面要贴一块与门板同色的板，否则看到的是白色刨花板边。

排布器早就算出了填缝条的位置与宽度，但它们**从来没进过报价单**——客户拿到的是一份
缺料的价格，装到一半才发现少东西。物料清单由 `src/layout/bom.ts` 统一生成；
公司规格库里缺哪类辅料型号就**如实报出来**，不静默跳过。

五金与配件只加在柜体上：给填缝条配 soft-close 铰链是没有意义的。

#### FR-6.3　换花色比价（v0.7 新增）

报价清单末尾给出**同一套方案换其他门板花色**的重算价：

```
【换一种门板花色】
  当前：Shaker White（价格组 A）        $1,358.50
  ────────────────────────────────────────────────
  Shaker Grey  （价格组 A）  同价      $1,358.50    ±$0
  Maple Glaze  （价格组 B）  贵 62%    $2,105.20    +$746.70
  Navy Shaker  （价格组 B）  —— 该商家的 SB36 不供应此花色，换色需调整水槽柜型号

  说明：以上是同一套方案换门板后的重算价，柜体数量与排布完全不变。
       填缝条、收口板、踢脚板会跟着换色，所以也在内；塑料地脚不随花色变。
```

四条实现规则，每条都有理由：

1. **逐行重算，不给总价乘系数。** 价格矩阵是逐格给的，不同型号在不同价格组之间的
   比例不一定相同。乘系数得到的数字会与真正下单时的数字对不上——那是最坏的一种
   "差不多"（与 FR-8「能算的就不要估」同源）。
2. **只换价格组，不重排。** 让客户看到的是**纯粹的花色差价**，不混进排布变化的差额。
3. **有价格洞的花色要标出来，不能悄悄跳过。** 商家可能对某型号不供应某花色
   （fixtures 里那个故意留的空洞正是为了测这条）。
4. **折扣、运费、税一并重算。** 满额折扣有门槛，换花色可能跨过门槛；只算柜体差价
   会误导。

### FR-7　报价发送

- 发送内容：四视图拼图 + 价格明细表格，通过 HTML 邮件发送（CID 内嵌图片 + HTML 表格 + 纯
  文本兜底版本 + 图片附件兜底），详见 SCENARIOS.md 场景 E。
- 发送目标为公司登记的报价邮箱。
- 每次成功发送**可能**生成一条 `LeadBillingEvent`（受去重窗口约束，见第 8 节与 FR-7 计费小节）。

#### 计费事件的生成规则（v0.4 新增）

- **去重窗口**：同一 `(companyId, conversationId)` 在 `LEAD_DEDUPE_WINDOW_DAYS`（默认 **30 天**）
  内只产生**一条**计费事件。窗口内的重复发送**照常发邮件**（客户改了设计再发一次是正常行为），
  但不再产生新的计费事件，只记一条 `QuoteAuditEvent(suppressedDuplicateBilling)`。
- **去重键**：`dedupeKey = hash(companyId, conversationId, windowBucket)`，数据库层加唯一约束
  兜底——不能只靠应用代码"记得判重"（与 3.4 节的双重防线原则一致）。
- **争议窗口**：发送后 `LEAD_DISPUTE_WINDOW_DAYS`（默认 **14 天**）内，公司可对该条线索提起
  争议。争议窗口内 `feeStatus` 不得进入 `invoiced`。

### FR-8　发送闸门与数据隔离（硬性要求，v0.4 强化）

**（1）价格不由 LLM 产生**

- LLM 的输出只允许包含**选择**：`{ moduleId, qty, width, height, depth, doorStyleId,
  hardwareOptionIds[], accessoryOptionIds[] }`。
- 输出中任何形如价格/金额/小计/总价的字段**一律丢弃**，不进入任何计算。
- 所有金额由 `PricingEngine` 从 `published` 规格版本计算得出（见 3.5.4）。
- 理由：只校验"型号 id 存在"挡不住"存在的型号 id + 编造的价格"——后者更危险，因为它能通过
  v0.3 版本的全部校验并被发送出去。

**（2）生成 `Quote` 前的硬校验清单（全部通过才允许继续）**

1. `moduleId` 存在于该 `companyId` 的 **`published`** `ProductSpecVersion`。
2. 选定的 `width` / `height` / `depth` 落在该型号的离散候选集内（不允许"接近的尺寸"）。
3. `doorStyleId` 属于该公司且在同一版本内；其 `priceGroupId` 在 `PriceMatrixEntry` 里对该
   型号**存在条目**（矩阵可能有空洞：某些型号不供应某些价格组）。
4. 五金/配件选项属于该公司、在同一版本内、且适用于该型号类型。
5. 客户所在省份已确定，且能查到当时生效的 `TaxRule`。
6. 引擎重算的金额与将要写入的快照**逐项一致**。
- 任一项失败 → **拒绝生成**，记录 `QuoteAuditEvent(validationRejected)` 与失败原因，
  不进入发送闸门。不做"自动修正后继续"。

**（3）发送闸门是服务端状态机**

- `Quote.status` 只能按 `draft → confirmed → sent` 迁移，迁移由**独立的服务端 API 动作**驱动，
  每次迁移写 `QuoteAuditEvent`。
- **不接受请求体里的 `confirm: true` 布尔字段作为闸门**——v0.3 沿用的这个做法等于把闸门交给
  调用方，任何能访问接口的人都能绕过。客户确认必须是一次留痕的服务端状态变更。
- 未配置发信凭据时仍然是 dry-run（这一道保留），但它是**第二道**，不是唯一一道。
- 发送前必须向客户展示"以下信息将发送给 X 公司"的明确披露（见 FR-13）。

**（4）公司间数据隔离**

- 生成的设计/报价单只能引用该公司规格库中真实存在的实体 id（型号、门板、选项），
  引用不存在的 id 时代码层面直接拒绝。
- **只有绑定了真实 `CabinetCompany` 的 `Quote` 才能进入发送闸门**——`EstimateDraft`
  （冷启动通用预估，见 FR-10）没有 `companyId`、也就没有收件地址，天然无法被发送，
  不需要额外加规则去防止误发。

### FR-14　交付前审核（v0.8 新增，已实现）

**在把任何结果交给客户之前，按约束与规则严格过一遍。**

#### 为什么要单独有这么一层

检查散在各处，每一处都只看自己那一块：排布器检查人体工程、BOM 检查缺不缺料、
报价清单检查逐行对不对得上小计、定价引擎检查价格矩阵有没有洞。每一处都对，
但**没有任何一个地方回答「这一份东西现在能不能给客户」**。

于是出现过这样的组合：

| 组合 | 每一步都成功，但结果是 |
|---|---|
| 方案过了人体工程，物料清单缺一条踢脚板 | 客户拿到一份**装不上**的报价 |
| 报价算出来了，逐行合计与小计差 3 分 | 客户拿去跟别家比，比的是个**错数** |
| 家电尺寸是我们猜的，解释里没说 | 客户按图订柜，**冰箱塞不进去** |

这三种都不是"某个模块有 bug"，而是**没人在最后一道口子上把它们合起来看**。

#### 闸门，不是评分

与 FR-4.1 的三层结构同源：

- **`blocking`**：一条都不允许放行，**不参与权衡**。理由和硬约束一样——
  任何权重都能被"其他方面都很好"投票压过去，而一份缺料的报价单不会因为
  方案好看就变得能用。
- **`advisory`**：不拦，但**必须显示给客户**。它们不是日志，是交付物的一部分：
  「你选的组装方式有几个柜体不提供」如果只写进服务端日志，客户永远不会知道。

#### 检查清单

| 代码 | 严重度 | 查什么 |
|---|---|---|
| `STAGE` | blocking | 这个阶段该不该出这份东西（与 FR-4.2 的阶段机同源） |
| `ERGONOMICS` | blocking | 人体工程与安全的 blocking 违规 |
| `GEOMETRY` | blocking | 柜体超墙 / **同一段墙内**互相重叠 |
| `INTERFERENCE` | blocking | **跨墙段**干涉：两段墙的柜子在墙角占同一块地方（v0.9） |
| `BOM_INCOMPLETE` | blocking | 物料清单缺型号 |
| `SPEC_MISMATCH` | blocking | 清单里出现规格库外的型号或尺寸（FR-8 同源） |
| `QUOTE_RECONCILIATION` | blocking | 逐行合计与小计对不上 |
| `PRICE_SNAPSHOT` | blocking | 报价快照复算不一致（§3.6） |
| `UNDISCLOSED_ASSUMPTION` | blocking | 推定值没有在**交给客户的文字里**披露 |
| `UNAPPLIED_PREFERENCE` | advisory | 客户的选择没能全部落实 |
| `AESTHETICS` | advisory | 排布评分偏低 |

#### 三条实现规则

1. **这层不重新实现任何检查。** 它调用已有的检查器并汇总结论。重新实现一遍
   等于制造第二个真相来源——两套规则迟早会分叉，而分叉时没人知道该信哪一个。
   `INTERFERENCE` 也遵守这一条：它读 `plan-model.ts` 拼出来的那张平面，
   与画图、排布让位读的是同一份。图上没干涉，就是真的没干涉。

2. **`UNDISCLOSED_ASSUMPTION` 查的是文字，不是数据字段。**
   `provenance: "assumed"` 存在数据里，但客户读到的是文字——两者可能脱节，
   而脱节的那一次正是出事的那一次。所以审核要拿到**将要交给客户的全部文字**，
   核对该说的话真的说了。

3. **报告里要写清「查了哪几项」。** 「没查」和「查过没问题」不是一回事。
   拦下来时也要说清拦的是什么——「报价校验未通过」对客户毫无用处。

#### 落点

`src/delivery/audit.ts`。三个交付口子各跑一次，查得一次比一次全：

| 交付物 | 跑的项 |
|---|---|
| 全局俯视图 | 阶段、人体工程、几何、排布评分 |
| 完整四视图 | 以上 + 物料完整、型号与尺寸同源、推定值披露、偏好落实 |
| 报价清单 | 以上 + 逐行对账、价格快照复算 |

报价这一步查得最严，因为它是客户**据以做决定**的东西：拿去跟别家比、拿去下单。
不通过时返回 409 并附上完整的审核结论。

### FR-9　多租户架构与账号体系

- 供给侧：每个 `CabinetCompany` 一个租户，数据严格按 `companyId` 隔离，应用层 + 数据访问层
  双重强制（见 3.4）。
- 需求侧：`CustomerAccount` 分 `consumer`（默认免费）/ `trade`（付费专业账号）；同一账号可拥有
  多个 `Conversation`。
- 每家公司的 Agent 是同一套引擎按 `companyId` 参数化调用，不是独立部署实例。

### FR-10　冷启动通用层

- 平台维护一份不属于任何具体公司的 `GenericCatalog`（行业典型尺寸与报价区间）。
- 客户即使在没有任何相关公司入驻/被 @ 的情况下，也能走"聊需求 → 出方案"的流程，得到一份
  `EstimateDraft`——**明确标注"行业典型区间，非具体公司真实报价"**，给区间不给误导性的精确
  数字（避免"报低吸引、真实报价却高很多"的信任风险，这类问题在同类平台上是真实发生过的）。
- `EstimateDraft` 与 `Quote` 是不同实体（见 FR-8），不进入发送闸门。
- 客户 `@` 一个尚未 `active` 的公司名字时，系统记录一条 `CompanyMentionSignal`（供销售外联
  使用），不向客户暴露"这家还没入驻/还没订阅"这类内部运营状态。

**（v0.4 修正 —— 与旧版场景 A 的矛盾已消除）**

未订阅（或已发布规格但未订阅）的公司，客户**可以 `@`**，交互不中断，但拿到的是
`EstimateDraft` 而不是绑定该公司的 `Quote`：

| 公司状态 | 客户能否 `@` | 客户拿到什么 | 是否记 `CompanyMentionSignal` |
|---|---|---|---|
| 不存在于系统 | 可以（不报错） | `EstimateDraft` | 是（`unknown`） |
| `prospect` | 可以 | `EstimateDraft` | 是（`matchedProspect`） |
| `onboarding` | 可以 | `EstimateDraft` | 是（`matchedProspect`） |
| 已发布规格但未订阅 | 可以 | `EstimateDraft` | 是（`matchedInactive`） |
| `active`（已发布 + 已订阅） | 可以 | 绑定该公司的 `Quote` | 否 |

选这个口径而不是"不可 `@`"的理由：销售外联时"已经有真实客户点名找你们，而且他手上已经拿到
一版通用预估了"比"有人提过你们"有力得多——这正是第 8 节冷启动转化逻辑的抓手。

### FR-11　专业/贸易账号（Trade Account）

- `trade` 账号支持在一个账号下管理多个 `Conversation`（多项目并行）。
- 贸易价通过 3.5 的 `DiscountRule`（`audience: trade`）或 `PriceMatrixEntry.tradePrice` 表达；
  `PricingEngine` 按 `CustomerAccount.accountType` 自动选用，并把所用规则记入报价快照。
- `trade` 账号的对话交互更"直给"（一轮问全、不解释术语、不反复确认），**差异只在问法上，
  不在能力上**——报价、校验、发送闸门一律与消费者相同。选择题（FR-1.1）每轮出几道也按这个
  参数走。
- `trade` 账号本身可付费订阅，与公司侧的线索费/个性化订阅是两条独立收入线，互不影响、可叠加
  （即：一条由 trade 账号发起的线索，仍然会对应生成公司侧的 `LeadBillingEvent`）。

### FR-12　公司发现与邮件列表（v0.4 新增）

详细讨论见 [COMPANY_DISCOVERY.md](./COMPANY_DISCOVERY.md)。

- **公司发现库（`CompanyProspect`）**：平台运营维护的市场库，来源是网络检索抓取
  （由管理员后台**手动触发**，非定时自动跑）+ 手工录入。支持增/改/删（删为归档，保留历史）
  与按 email 去重（已存在则更新 `lastUpdated`，保留原 `importedAt`）。
  MVP 阶段商家数量不大，用**平面文件持久化**（JSON），不引入数据库。
- **用途**：① 社媒广告的受众定向与地域投放决策；② `CompanyMentionSignal` 的回溯匹配数据源；
  ③ 市场情报（玩家数量、地域分布、覆盖缺口）。
- **明确不用于**：向抓取来的邮箱做任何形式的冷邮件群发。
- **合法获客路径**：社媒广告 → 企业主动访问平台 → 主动勾选同意后注册邮件列表
  （`EmailSubscription`，记录同意时间/渠道/IP/UA）→ 平台方可向其发送招商与产品邮件。
  每封邮件必须带可用的退订链接，退订请求 10 个工作日内生效（CASL 要求）。
- **客户端不再提供"选择多家公司群发询价"功能**——该功能在 v0.3 的现有代码里存在，与 CASL
  冲突，本版删除（见第 10 节）。

### FR-13　隐私、同意与数据留存（PIPEDA，v0.4 新增）

v0.3 的合规只覆盖了 CASL（发信环节）。但平台收集的户型图、需求描述、联系方式、以及
"谁在找哪家公司"这类信息属于**个人信息**，适用加拿大 PIPEDA，必须单独成条。

**（1）告知与同意（三类主体）**

| 主体 | 需要明确告知并取得同意的内容 |
|---|---|
| 消费者账号 | 收集什么（户型图、需求描述、联系方式、对话内容）、用途（生成设计与报价）、**会在客户确认后披露给客户选定的橱柜公司**、留存期限、如何撤回与删除 |
| trade 账号 | 同上，外加所属公司名称等商业信息的用途 |
| 橱柜公司 | 平台会存储其价目表等**商业机密级**资料、租户隔离承诺、会收到哪些客户信息、计费与争议规则 |

- 注册环节的同意必须是**主动勾选**（不得默认勾选、不得用"继续即代表同意"替代关键披露）。
- 同意记录需可追溯：时间、渠道、条款版本号。条款更新且涉及用途变更时需重新取得同意。

**（2）发送前的二次披露（与 FR-8 闸门绑定）**

客户点"确认发送"之前，界面必须逐项列出**将要发送给该公司的具体内容**（设计图、尺寸、
需求摘要、联系方式），并说明该公司会因此获得联系客户的能力。这是 PIPEDA 意义上的
"披露给第三方"，不能藏在通用条款里一笔带过。

**（3）销售外联中的去标识化（硬规则）**

`CompanyMentionSignal` 用于销售外联时，**不得向未入驻公司透露客户身份或联系方式**。
外联话术只能使用聚合/去标识信息（"近期有客户在平台上点名寻找贵司"），不得包含姓名、
邮箱、电话、具体地址。要把真实客户线索交给该公司，必须走正常的 `Quote` 发送闸门
（即客户自己确认发送）。

**（4）留存与删除**

| 数据 | 默认留存 | 说明 |
|---|---|---|
| 户型图、上传文件 | 24 个月 | 到期自动清除 |
| 对话记录、需求摘要 | 24 个月 | 同上 |
| 账号注销后 | 30 天内清除上述内容 | 保留期用于误操作恢复 |
| `Quote`、`LeadBillingEvent`、发票 | 7 年 | 财务/税务法定留存，**去标识化后保存**（去除客户姓名与联系方式，保留金额与型号明细）|
| `QuoteAuditEvent` | 7 年 | 同上，争议处理凭据 |

**（5）数据主体权利**

提供访问、更正、删除、撤回同意的入口；**30 天内响应**（PIPEDA 要求）。删除请求与上表的
法定留存冲突时，执行"去标识化保留"而不是拒绝删除，并向请求人说明。

---

## 6. 数据模型草案（v0.4 重构）

> 与 v0.3 相比的主要变化：`ProductSpec` → `ProductSpecVersion`（版本化）；新增价格组 /
> 门板样式 / 价格矩阵 / 折扣 / 运费 / 税率六个定价实体；`Quote` 增加快照与税费字段；
> 新增修订链与审计事件；`LeadBillingEvent` 增加去重与争议；`CompanyMentionSignal` 增加归一化。

### 6.1 需求侧

```
CustomerAccount
  id, accountType: consumer | trade
  email, displayName
  province                          # 新增：定价必需（决定税率）
  # trade 专属：
  companyName?
  subscriptionStatus?: none | trial | active
  consentRecords: [{ termsVersion, consentedAt, channel }]   # 新增：FR-13

Conversation
  id, customerAccountId
  messages[]
  designRequirements
  perCompanyThreads: [{ companyId, messages[] }]
```

### 6.2 供给侧：公司与规格版本

```
CabinetCompany
  id, name
  aliases: string[]                 # 新增：确定性 @ 路由用（FR-1）
  quoteEmail, website, contactName?
  province, serviceAreas: string[]  # 新增：province 决定默认税区
  status: prospect | onboarding | active | inactive
    # prospect   = 已知但未入驻（可能来自 CompanyProspect 或被客户 @ 过）
    # onboarding = 入驻会话进行中（FR-2）
    # active     = 规格已发布 且 已订阅个性化服务 → 可产生 Quote
    # inactive   = 曾经 active，现停用
    # 注：status 应由「是否存在 published 规格版本」+「订阅状态」派生，
    #     不作为可独立编辑的字段，避免两处真相不一致
  billingPlan: { leadFeeEnabled: boolean,
                 personalizationSubscription: none|trial|active }
  currentPublishedSpecVersionId?    # 新增：指向当前生效版本

ProductSpecVersion (属于某个 CabinetCompany)     # 改：版本化，发布后不可变
  id, companyId
  versionNo                         # 单调递增
  status: draft | published | archived
  currency: "CAD"
  effectiveFrom, effectiveTo?
  publishedAt?, publishedBy?
  changeNote?
  # 约束：同一 companyId 下 published 最多 1 个、draft 最多 1 个
```

### 6.3 供给侧：规格内容（均隶属某个 ProductSpecVersion）

```
ModuleSpec
  id, specVersionId, companyId
  code                              # "B12" / "W2430" / "SB36"
  type: base|wall|tall|corner|sinkBase|filler|panel|toeKick|crown
  widthOptions[], heightOptions[], depthOptions[]    # 离散英寸值
  faceTemplateId                    # → 平台共享 FaceTemplate
  assemblyOptions: ("RTA"|"assembled")[]

PriceGroup
  id, specVersionId, companyId
  code                              # "A" / "B" / "Premium"
  displayName, rank

DoorStyle
  id, specVersionId, companyId
  name                              # "Shaker White"
  priceGroupId                      # 每种门板属于且仅属于一个价格组
  material?, color?, imageRef?

PriceMatrixEntry                    # ★ 价格的唯一真相来源
  specVersionId, companyId
  moduleId, priceGroupId            # 复合主键
  listPrice                         # RTA（未组装）标价
  assembledUpcharge?: { kind: "flat"|"percent", value }
  tradePrice?                       # 少数公司维护独立贸易价矩阵时使用
  # 矩阵允许有空洞：某型号不供应某价格组 → 无此条目 → FR-8 校验会拒绝该组合

HardwareOption
  id, specVersionId, companyId
  name                              # "Soft-close hinge"
  priceModifier: { kind: "flat"|"percent", value }
  appliesToModuleTypes[]?

AccessoryOption
  id, specVersionId, companyId
  name                              # "Roll-out tray"
  priceModifier: { kind: "flat"|"percent", value }
  appliesToModuleIds[]?

DiscountRule
  id, specVersionId, companyId
  audience: retail | trade
  kind: "percentOffList" | "tieredByOrderValue"
  value?                            # percentOffList 用
  tiers?: [{ minSubtotal, percentOff }]              # tieredByOrderValue 用
  appliesToPriceGroupIds[]?         # 留空 = 全部价格组
  stackable: boolean                # 是否可与其他规则叠加

ShippingRule
  id, specVersionId, companyId
  kind: "flat" | "freeOver" | "byRegion"
  flatAmount? | freeOverThreshold? + belowThresholdAmount? | regionTable?
```

### 6.4 平台共享层（不属于任何租户）

```
FaceTemplate
  id, view: front|topBase|topWall|side
  category: singleDoor|doubleDoor|drawers|doorDrawerCombo|corner|sinkBase|openShelf|...
  svgSymbol

GenericCatalog                      # 冷启动用的行业基准目录
  id
  modules: [{ type, widthOptions[], heightOptions[], depthOptions[], typicalPriceRange }]

TaxRule                             # 新增：加拿大各省税率，带生效区间（见 3.5.3）
  id, province
  components: [{ name: "GST"|"PST"|"HST"|"QST", ratePercent }]
  compounded: boolean
  effectiveFrom, effectiveTo?
```

### 6.5 设计与报价

```
FloorPlan
  id, conversationId, sourceFile, parsedGeometry
  parseConfidence, unresolvedItems[]              # 新增：完整性优先（FR-3）

DesignLayout (属于某个 FloorPlan + 某个 CabinetCompany)
  id, floorPlanId, companyId, specVersionId       # 新增：绑定规格版本
  currentRevisionNo
  status: draft | confirmed

DesignRevision                      # 新增：修订链，不覆盖历史（3.6）
  id, designLayoutId, revisionNo
  placedModules: [{ moduleId, position, dimensions, doorStyleId,
                    hardwareOptionIds[], accessoryOptionIds[] }]
  views: { front, topBase, topWall, side }
  createdAt, triggeredBy: "auto" | "customerRequest"
  changeSummary

Quote (属于某个 DesignRevision，绑定真实 CabinetCompany)
  id, designLayoutId, designRevisionNo, companyId, conversationId, customerAccountId
  # ── 快照字段（3.6）───────────────────────────────
  specVersionId                     # 冻结：本单基于哪一版规格
  accountTypeAtQuote: consumer | trade
  doorStyleId, priceGroupId         # 整单落在哪个价格组
  currency: "CAD", province
  taxRuleSnapshot, shippingRuleSnapshot, appliedDiscountRuleIds[]
  # ── 明细 ─────────────────────────────────────────
  lineItems: [{
      moduleId, moduleCode, qty,
      width, height, depth,                       # 必须在离散候选集内（FR-8 校验 2）
      assembly: "RTA"|"assembled",
      unitListPrice,                              # 快照
      modifiers: [{ kind, refId, name, amount }], # 五金/配件/组装加价，快照
      unitNetPrice, lineSubtotal
  }]
  subtotal
  discounts: [{ ruleId, description, amount }]
  shipping: { ruleId?, amount }
  taxes: [{ name, ratePercent, amount }]          # 分项列出（FR-6）
  total
  validUntil                                       # 报价有效期
  status: draft | confirmed | sent | failed | expired
  # 状态迁移只能由服务端 API 驱动，每次迁移写 QuoteAuditEvent（FR-8）

QuoteAuditEvent                     # 新增：可追溯（3.6）
  id, quoteId, companyId
  at, actor: customer | system | operator
  action: created | revised | validationRejected | confirmed
        | sent | sendFailed | suppressedDuplicateBilling | expired
  contentHash                       # 当时报价内容的哈希
  details

EstimateDraft                       # 冷启动通用预估，与 Quote 严格区分
  id, conversationId
  basedOn: "genericCatalog"
  lineItems: [{ moduleType, qty, estimatedPriceRange }]
  totalRange: { low, high }
  disclaimer: string                # "行业典型区间，非具体公司真实报价"
  # 无 companyId → 天然无法进入发送闸门（没有收件地址）

QuoteComparison
  id, conversationId
  quotes: [{ companyId, quoteId, total, taxIncluded: boolean }]   # 新增含税口径标记
```

### 6.6 销售信号与计费

```
CompanyMentionSignal                # 改：增加归一化与实体关联（v0.4）
  id, conversationId, customerAccountId
  rawMentionText                    # 用户原话
  normalizedName                    # 规范化：去空格/统一大小写/去 Inc·Ltd·有限公司/简繁统一
  resolvedCompanyId?                # 命中已存在的 CabinetCompany
  resolvedProspectId?               # 命中市场库 CompanyProspect
  resolutionStatus: matchedActive | matchedInactive | matchedProspect | unknown
  createdAt
  # 销售看板按 normalizedName 聚合计数，避免 "IKEA"/"宜家"/"宜家家居" 碎成三条

LeadBillingEvent                    # 改：增加去重与争议（v0.4）
  id, companyId, conversationId, quoteId, customerAccountId
  sentAt
  dedupeKey                         # hash(companyId, conversationId, windowBucket)
                                    # 数据库唯一约束兜底（FR-7）
  feeAmount, currency
  feeStatus: pending | invoiced | paid | disputed | refunded | waived
  dispute?: {
    openedAt, openedBy,
    reason: duplicate | invalidContact | outOfServiceArea | spam | other,
    evidence,
    resolvedAt?, resolvedBy?,
    resolution?: upheld | refunded | partialRefund
  }
```

### 6.7 公司发现与邮件列表（FR-12，平面文件持久化）

```
CompanyProspect                     # data/companies.json
  id, name, email, website, phone?, city, province
  sourceType: "web_scrape" | "manual"
  importedAt, lastUpdated
  status: prospect | contacted | subscribed | archived
  notes

EmailSubscription                   # data/subscriptions.json
  id, email, companyName
  consentDate, consentChannel: "web_form"
  termsVersion, userAgent, ipAddress(脱敏)
  unsubscribeToken
  status: active | unsubscribed
  unsubscribedAt?
```

---

## 7. 非功能性需求

- **数据隔离**：见 FR-8/FR-9，程序层面硬校验，且要在数据访问层兜底（不只靠应用层"记得过滤"），
  应用层 + 数据层双重防线。
- **可追溯**（v0.4 落到数据模型）：
  - 规格版本化（`ProductSpecVersion` 发布后不可变）；
  - 报价冻结价格快照 + `specVersionId`，任何时刻可复算校验；
  - 设计修订链 `DesignRevision` 不覆盖历史；
  - 关键动作写 `QuoteAuditEvent`（含内容哈希），保留 7 年。
- **定价正确性**：`PricingEngine` 是纯函数，必须有覆盖以下场景的单元测试——多价格组、
  组装加价、百分比与定额修饰项混合、分档折扣边界值、各省税率（含 QC 的 QST 与 NS 的税率变更）、
  运费阈值边界、报价过期。
- **可扩展**：新公司入驻 = 走一遍"公司入驻规格录入会话"或模板导入即可上线，不需要改代码；
  税率变更、费率调整均通过数据而非代码变更。
- **合规**：CASL（发信，见 FR-12）+ PIPEDA（个人信息，见 FR-13）。
- **隐私与留存**：见 FR-13 第 (4) 条的留存表；到期清除必须是自动任务，不能靠人工记得。

---

## 8. 商业模式与货币化策略

参考 Angi/HomeAdvisor、Thumbtack、Houzz Pro 三家同赛道公司的真实收入结构后确认：

- **需求侧（消费者默认账号）永远免费**——这是同类平台的共同惯例；即使是已有付费会员产品的
  Angi，会员费也只占其总收入约 3.8%，主力收入始终在商家侧，这一点不是保守判断，是行业常态。
- **供给侧（橱柜公司）是主要收费对象，第一阶段主模式是"按线索付费"**（对应 `LeadBillingEvent`，
  触发点就是产品里已有的"确认发送"动作）——这个模式对首批公司销售阻力最小（不用预付费才能看
  到效果），且 RTA-Hub 的线索天然**绑定单一公司、不共享给竞争对手**（不像 Angi 那样一条线索卖
  给 3-8 家互相竞争的服务商），不需要靠会员费去补贴线索定价。
- **公司的"个性化订阅"是第二层转化**：公司完成入驻但未订阅时，客户端对该公司只能拿到冷启动
  通用层的 `EstimateDraft`（FR-10）；订阅后，才产生绑定该公司真实规格库的 `Quote`。这份通用层
  预估同时是解决"双边市场先有鸡还是先有蛋"冷启动难题的抓手——用一个已经存在的真实客户需求
  （哪怕只是通用预估）去说服公司入驻付费，比空口叫公司"先付费再看有没有线索"阻力小得多。
- **需求侧的 trade 账号（FR-11）是第三条独立收入线**——建商/装修公司这类专业用户本身有付费
  意愿（省自己的时间成本，逻辑上类似 Houzz Pro，只是用在需求侧而非供给侧），与前两条收费线
  互不冲突、可叠加计费。
- **成交抽佣模式暂不作为设计目标**——平台目前没有办法验证线下是否真实成交，留作后续（若引入
  支付/履约环节）的演化方向，不纳入当前架构设计。

### 8.1 线索计费的去重与争议（v0.4 新增）

同类平台上，商家侧最大的抱怨来源就是"无效线索还要收费"。这不是可以推迟到以后再补的运营细节，
按线索付费一旦是主模式，争议流程就是它的必备件：

- **去重**：同一客户对同一公司的重复发送，30 天窗口内只计费一次（规则见 FR-7）。
- **争议受理**：发送后 14 天内公司可提起争议，理由分类为
  `duplicate | invalidContact | outOfServiceArea | spam | other`。
- **争议期内不出账**：`feeStatus` 停留在 `pending`/`disputed`，不进入 `invoiced`。
- **裁定依据**：`QuoteAuditEvent`（含内容哈希）+ 邮件投递回执，由平台运营裁定
  `upheld | refunded | partialRefund`，结果与理由需回复给公司。
- **透明度**：公司后台可查看自己每一条计费事件的来源对话摘要（去标识化，见 FR-13 第 (3) 条）
  与当时的报价内容哈希。

---

## 9. MVP 分期建议（v0.4 更新）

多租户（FR-9）不是一个独立的后期里程碑，是从 MVP-1 开始就要落地的架构基础（租户隔离、账号
类型），不是"以后再加"的功能。

### MVP-1

**目标：用 1 家真实公司把"规格录入 → 对话 → 报价 → 确认发送 → 计费"这条链路完整跑通。**

- 多租户基础架构（`CabinetCompany` / `CustomerAccount` 隔离，见 FR-9）。
- **FR-2 公司入驻规格录入会话，先跑通 1 家**（v0.4 调整：原为 2-3 家）——同时实现
  **结构化模板导入的兜底路径**，确保规格录入不会卡住整条链路（见 FR-2 降级方案）。
- 定价模型（3.5）与 `PricingEngine` 完整落地：价格组、门板样式、价格矩阵、修饰项、折扣、
  运费、税。**这是 MVP-1 的硬性内容**——报价单不含税/不含运费就不是一份能给客户看的单子。
- 规格版本化与报价快照（3.6）：`ProductSpecVersion` + `Quote` 快照 + `QuoteAuditEvent`。
  版本化如果不在第一版做，以后迁移历史报价的代价很高。
- 客户端多 Agent 对话（总控 + `@` 确定性路由，见 FR-1）。
- 报价单生成 + FR-8 全部硬校验 + 服务端状态机发送闸门。
- `LeadBillingEvent` 计费事件记录，含去重键与争议字段（争议**处理界面**可后置，但字段与
  去重约束要在第一版落地）。
- 冷启动通用层的**纯文本版** `EstimateDraft`（FR-10，不含视图渲染——四视图依赖 MVP-2 的
  排布/模板能力）。
- FR-13 的注册同意、发送前二次披露、留存策略配置。
- `CustomerAccount.accountType` 与贸易折扣规则在数据模型里一并落地（成本低，避免以后痛苦
  迁移），但 trade 账号的完整交互体验（多项目管理、专属交互模式）不在 MVP-1 范围。
- FR-12 的公司发现库（平面文件 + 管理员后台手动触发抓取 + 增删改查）与邮件列表注册。

**MVP-1 出口条件**：1 家试点公司通过 FR-2 验收标准（见 FR-2 量化验收表）；一条真实客户对话
能走到"确认发送"并产生 1 条计费事件；报价金额经人工逐项核对无误。

### MVP-2

- 户型图上传解析 + 自动排布算法 + 脸型模板库（3.3 节）+ 四视图渲染。
- 多公司价格对比（含税口径标注）。
- HTML 邮件发送（内嵌图 + 表格）。
- `EstimateDraft` 升级为**含四视图版本**（复用同一套脸型模板渲染）。
- 试点公司扩到 2-3 家，验证 FR-2 的可复制性。
- 计费争议的处理界面。

### MVP-3

- PDF 正式报价单留白件。
- trade 账号的多项目管理界面与专属交互模式。
- 体验打磨、（如需要）更复杂排布约束。
- 若商家规模增长，`CompanyProspect` / `EmailSubscription` 从平面文件迁到数据库。

---

## 10. 与现有代码的关系

| 现有模块 | 处置建议 |
|---|---|
| `leads.ts` | **重新定位为运营工具**（见 [COMPANY_DISCOVERY.md](./COMPANY_DISCOVERY.md)）。爬虫能力保留，改成管理员后台手动触发的任务；数据存入 `CompanyProspect`（平面文件）；不再用于消费者群发。用途：社媒投放的受众定向、`CompanyMentionSignal` 回溯、市场情报。已知需修复：`MAX_FETCH_BYTES` 限流失效（响应体已全量进内存后才切片）、无私网地址过滤（SSRF） |
| `email.ts` | 发送闸门的 dry-run 机制保留复用；但 `confirm` 布尔参数必须换成服务端状态机（FR-8 第 3 条）。邮件类型分化为：① `LeadEmail`（`Quote` 发送，FR-7）② `InviteEmail`（招商邀约，FR-12）③ `MailingEmail`（订阅列表投递，FR-12）。内容从"通用询价信"改为"HTML 内嵌视图 + 表格"（FR-7） |
| `server.ts` / `web/index.html` | 架构层面接近重写：从"搜索列表 → 选中 → 群发草稿"改成"多 Agent 对话 + `@` 路由 + 户型图上传 + 视图展示 + 多租户账号体系"。**客户端的"选择多家公司群发询价"功能删除**（与 CASL 冲突，见 FR-12）。另需修复：全局模块级单例状态与多租户冲突、无鉴权、`/api/chat` 无错误处理、`requirements` 被空串覆盖（`??` 应为 `\|\|`） |

---

## 11. 已关闭的问题（历史归档）

**v0.1 → v0.2（7 项）**

1. 产品规格数据来源 → 公司入驻规格录入会话（FR-2）
2. 户型图尺寸获取 → 图像理解/OCR + 对话核对，1/4 英寸精度，不支持 CAD（FR-3）
3. 三视图渲染保真度 → 脸型模板库参数化拼接（3.3 节）
4. 排布自动化程度 → 全自动生成，仅对话式修改（FR-4）
5. 多公司对比 → 只做价格对比，不做图形对比（FR-6）
6. 报价单发送格式 → HTML 内嵌（图片 CID + HTML 表格）+ 纯文本兜底 + 图片附件兜底（FR-7）
7. 公司间数据隔离保障 → 程序层面硬校验型号 id（FR-8）

**v0.2 → v0.3**

8. 是否需要专门解析 2020 Design 等专业设计软件的导出文件 → 不单独做，并入 FR-3 的通用上传/
   抽取管线。
9. 系统是否应该是多租户架构 → 是，且是两个维度（供给侧公司租户 + 需求侧消费者/专业账号），
   见 3.4、FR-9。
10. 冷启动阶段（尚无公司入驻）如何让客户端仍有产品体验 → 通用层 `EstimateDraft` + 公司订阅后
    解锁个性化 `Quote` 的两层转化路径，见 FR-10、第 8 节。
11. 小型建商/装修公司这类专业用户的使用模式与商业模式 → 独立的 `trade` 账号类型（多项目管理 +
    贸易价可见），本身可付费订阅，与公司侧收费线独立叠加，见 FR-11、第 8 节。

**v0.3 → v0.4（本轮新增决策）**

12. 定价模型如何表达真实 RTA 价目表 → (型号 × 门板价格组) 价格矩阵 + 修饰项 + 折扣规则 +
    运费 + 分省税率，见 3.5、第 6 节。
13. "可追溯"如何落到数据模型 → 规格版本化 + 报价价格快照 + 设计修订链 + 审计事件，见 3.6。
14. 如何防止 LLM 编造价格 → 价格一律由 `PricingEngine` 计算，LLM 输出中的金额字段一律丢弃；
    新增 6 项硬校验清单，见 FR-8。
15. 发送闸门为什么不能用请求体里的 `confirm` 字段 → 改为服务端状态机 + 审计留痕，见 FR-8 (3)。
16. 已发布规格但未订阅的公司，客户能否 `@` → **可以**，拿到 `EstimateDraft`（以 FR-10 为准，
    场景 A 同步修正）。
17. 线索计费的重复与争议 → 30 天去重窗口（数据库唯一约束兜底）+ 14 天争议窗口 + 裁定流程，
    见 FR-7、8.1。
18. 合规是否只需覆盖 CASL → 否，个人信息适用 PIPEDA，新增 FR-13（告知同意、发送前二次披露、
    销售外联去标识化、留存与删除、数据主体权利）。
19. FR-2 的成功标准是什么 → 8 项量化验收指标（价格准确率 0 容错），并配套结构化模板导入的
    降级兜底，见 FR-2。
20. FR-2 是否放在 MVP-1 → 放，但试点公司从 2-3 家收敛为**先跑通 1 家**，且兜底路径同期实现。
21. `@` 路由由谁判断公司身份 → 确定性匹配（mention token / 别名精确匹配），LLM 不参与，
    见 FR-1。
22. `CompanyMentionSignal` 如何避免碎片化 → 增加 `normalizedName` 与实体关联字段，
    销售看板按归一化名聚合，见 6.6。
23. 爬虫功能的定位 → 从"消费者群发数据源"改为"平台运营的市场发现库"，合法获客改走
    社媒广告 → 主动订阅路径，见 FR-12、COMPANY_DISCOVERY.md。
24. 脸型模板由谁绘制 → 不绘制。声明式脸型文法 + 代码生成，模板是数据（15 正视 + 4 俯视 +
    4 侧视）；并否决"归一化 symbol + 缩放"的实现方式（会放大门缝、扭曲线宽），
    见 [RENDERING.md](./RENDERING.md)。

---

## 12. 开放问题（v0.5 更新）

### 本轮关闭

**2. PDF 正式报价单的优先级** → **已关闭**：确认放 MVP-3，且已实现。

实现要点（`src/pdf/`）：零依赖的 PDF 1.4 写入器 + base-14 标准字体。立面图**直接从
`Placement` 的绝对英寸坐标画成 PDF 矢量指令**，不经过"先出 SVG 再转换"——几何本来就在手上，
转换一道只会引入误差与一个第三方依赖。

一个已知边界，如实记在这里：base-14 字体用 WinAnsiEncoding，画不出中文。报价单的收件方是
加拿大橱柜公司，正文本就是英文；户型解析出来的中文墙段标签（「北墙」）在 PDF 上会译成
`North Wall`，译不出的退到 `Wall N`，**不会留 `??`**。若将来公司名/客户名确实需要中文上纸，
要嵌入 CJK 字体（体积以 MB 计），届时再单独决策——现在不偷偷降级，替换字符一律进 `warnings`。

**3. 价格对比表的触发时机** → **已关闭**：**随时可拉，但必须标注口径**。

理由：等所有公司都确认完再给对比表，等于把最有价值的信息压到最后，而客户在中途本来就会问
"现在哪家便宜"。真正的风险不是"过早比价"，而是**拿口径不同的数字比**——A 家算了运费、
B 家还没选门板，直接比总价会误导。所以 `buildComparison` 的 `comparable` 标记与文案提示是
这个决策的执行点：口径不一致时对比表照出，但明确写出哪几项还没可比。

**4. FloorPlan 置信度低的兜底交互** → **已关闭**：**系统主动逐项追问，绝不自己猜**。

- 抽取不确定的项进 `FloorPlanUnresolved`，`suggestion` 字段**仅供展示、永不自动采用**；
- `isLayoutReady` 为假时，出方案接口直接 409 并把待办项列出来——不是警告，是硬闸门；
- 未配置视觉模型时整条录入降级为手动，而不是用低置信度的猜测继续往下走。

判断依据：户型尺寸错 3 英寸，出来的是一份装不上墙的方案和一份错的报价。这类错误发现得越晚
代价越大，让客户"自己发现不对再反馈"是把成本转嫁给最没有能力发现问题的一方。

**7. `trade` 账号的资质核实** → **已关闭**：**人工审核（营业执照 / GST 号）+ 付费订阅，
两道门槛都通过才给贸易价**。

关键在于**这个决策落在定价链路上，而不是界面上**：`effectiveAccountType()` 在生成报价时把
未过门槛的 `trade` 账号按 `consumer` 定价，所以绕过界面直接打 API 也拿不到贸易价
（`src/trade/verification.ts`）。降级时在响应里如实说明原因与下一步，不静默按零售价出单。

配套的隐私约束：**只存营业执照号/GST 号与注册名，不存证件影像**（FR-13 最小化原则）——
核实只需要一个可查证的号码，留存影像平白扩大泄露面。

### 仍未关闭

5. **`GenericCatalog` 的数据从哪来？** 行业公开价目表整理、还是平台自己按经验估算区间？
   数据来源影响 `EstimateDraft` 的可信度。**当前状态**：代码里有 `catalogSourceVerified`
   开关（默认 false），未核实时预估文案必须标注为占位数据——先把"未核实"这件事显式化，
   不让占位数字冒充行业数据。上线前必须关闭此项（检查清单 A4）。
6. **`LeadBillingEvent` 的费率怎么定？**（去重与争议规则已在 v0.4 关闭，费率本身仍开放）
   参考 Thumbtack 同类品类 $10-100+ 的量级，但橱柜单均金额高（一整套通常大几千到几万加币），
   费率结构可能需要与品类均价挂钩，而不是套用其他家装品类的定价。
7. ~~`trade` 账号资质核实~~ → 已关闭，见上。
8. **`CompanyMentionSignal` 触发后的销售外联流程**：归一化与聚合已解决，看板 API 也已具备
   （`/api/admin/mention-signals`，去标识化）。剩下的是**运营侧流程**问题——谁跟进、跟进节奏、
   转化如何记账，属于运营决策不是工程决策。
9. **社媒投放的预算与渠道选择**（FR-12）：LinkedIn（B2B 精准但单价高）vs Facebook/Instagram
   （覆盖广、本地商家活跃）的组合与预算分配，需要运营侧决策。
10. **NKBA 净空数值的正式核实**：代码里的净空常量（水槽两侧 24"/18"、灶台两侧 15"/12"、
    洗碗机距水槽 ≤36" 等）来自公开整理的 NKBA 指南，**尚未对照官方原文逐条核实**。
    这些是硬约束——数值错了会把本来合规的方案判为不合规，或者放过真正的问题。
    上线前必须核实（检查清单 A6，阻断项）。

---

请 review 本版新增的部分：**第 12 节关闭的四个开放问题**（PDF 优先级、对比表触发时机、
户型兜底交互、trade 资质核实）。MVP-1 至 MVP-3 的实现进度见
[DEV_PLAN.md](./DEV_PLAN.md)，上线前必须完成的核实项见
[PRE_LAUNCH_CHECKLIST.md](./PRE_LAUNCH_CHECKLIST.md)。
